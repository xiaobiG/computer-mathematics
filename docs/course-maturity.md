---
title: 课程成熟度看板
description: 公开展示五个专题的深度正文、可运行项目、下一步缺口与验证口径。
search: false
---

# 课程成熟度看板

所有 110 篇实际课程文章现已标记学习层级、前置知识、预计时长与配套实验；构建会检查这些字段。每篇也都具备至少 4 道练习及至少 4 条答案提示。下表只记录进一步满足深度文章标准的内容：具备完整推导/证明、代码、失败边界和分层练习。其余文章仍可作为导读，但不标记为深度正文。

## 当前概览

| 专题 | 实际课程 | 深度正文 | 可运行项目 | 下一关键缺口 |
| --- | --- | --- | --- |
| 预备知识 | 6 篇 | 符号、函数、有限求和与 Python 契约、集合/数组形状、张量形状与行批次、有限关系/邻接矩阵、可达闭包（含关系复合—布尔矩阵—路径长度归纳）、布尔矩阵乘法、稀疏两跳、位集合批次、版本缓存失效与冻结工作负载本机测量 | 数学预备实验室（含集合/数组、张量、关系闭包、布尔组合、批次、版本失效及运行时样本） | 并发缓存一致性与动态更新下的摊还复杂度 |
| 线性代数 | 18 篇 | 向量（含余弦/欧氏度量选择）、矩阵乘法（含精确/浮点结合路径对照）、基（含唯一坐标证明、增广秩可达性判据与最小二乘入口）、四个子空间（含 RREF 基、维数与正交补证书）、消元、LU、QR（含经典/改进 Gram–Schmidt 的正交缺陷对照）、最小二乘（含读者构造的可达性/秩诊断）、PCA（协方差半正定性、Rayleigh 商、重构误差与中心化 SVD 等价）、幂迭代（含初值盲区与残差/主特征对对照）、SVD（含精确秩/数值秩的缩放阈值对照）、低秩图像压缩与压缩域检索、MSE/PSNR、局部 SSIM、线性 RGB/sRGB 解码与 D65 CIE Lab 色差及显示条件→人工复核门槛、随机范围发现→随机 SVD（含草图/截断误差勾股分解）→MSE 预算评审的可验证产物流、低秩推荐与 ALS（含训练/留出误差对照）、Jacobian/Hessian | 线性代数实验室（含度量选择、矩阵乘法浮点结合对照、QR 正交缺陷比较、最小二乘建模诊断、幂迭代初值敏感性、SVD 数值秩缩放对照、低秩推荐留出评估、四子空间、随机范围发现→随机 SVD 误差分解→图像误差、局部 SSIM、线性 RGB、sRGB 解码、CIE Lab 与显示条件声明证书） | 实测显示设备与真实感知/任务评价 |
| 离散数学 | 23 篇 | 逻辑/归纳（含有界反例搜索）、集合/关系（含性质反例诊断）、循环不变量、复杂度（含全序数值前提）、动态数组扩/缩容（含滞回势能与任意前缀摊还界）、邻接表/邻接矩阵、BFS、DFS、强连通分量（含互达 oracle 与错误合并反例）、递推关系（含递归树分割、总工作与任意规模精确最坏比较重放）、Dijkstra、Bellman–Ford（含第 $|V|$ 轮负环见证）、Floyd–Warshall、同图最短路算法选择、单边插入增量最短路、单边删除重算审计、稳定边 ID 的顺序批量更新、最大流最小割（含反向残量重排与弱/强对偶证明）、并查集（含路径压缩重复查询对照）、贪心/动态规划（含读者任务合同、目标与验收不变量选择）、P/NP 与归约（含独立集到 CNF-SAT 构造） | 算法实验室（含逻辑反例搜索、关系反例、SCC 互达审查、Bellman–Ford 负环、动态数组扩/缩复制与势能轨迹、并查集路径压缩报告、可重放递归树与精确最坏比较、图表示、查询量、目标停止、密度/存储、更新失效、插入修复、删除与顺序批量重算审计、活动任务诊断、CNF 归约） | 替换路径数据结构、并发快照与批量更新的摊还复杂度 |
| 概率论 | 31 篇 | 概率空间与事件、贝叶斯（含相关证据的联合似然反例）、联合/边缘/条件分布、期望方差（含条件分解与直接混合的独立验算）、协方差相关性、大数定律/中心极限定理（含重复日志的有效样本量反例）、两状态 Markov 时间相关均值（含有限窗口精确方差/有效样本量）、AR(1) 相关区间（含精确方差、欠覆盖与声明 Bartlett HAC 对照）、抽样误差与置信区间（含区间到最小实际效应人工复核的跨课链）、常见分布（含右截尾 Kaplan–Meier/RMST）、最大似然（含经验交叉熵/KL 分解与 MLE/MAP/预测区分）、交叉熵/KL、共轭先验（含 Beta–Binomial 批量后验预测与方差对照）、多组 Beta–Binomial 部分汇聚（含收缩权重与可交换性边界）、假设检验、蒙特卡洛（含多种子/同目标重要性报告入口）、Metropolis–Hastings（含有限步初值/总变差对照）、概率校准、验证集再校准→成本敏感决策的可验证产物流、数据漂移（含点距离/样本波动对照）、带标签窗口性能审计、联合证据、子群体性能（含零样本声明组）、分组校准、冻结窗口比较、观测级/时间块/簇级/时间分层簇级/配对纵向 bootstrap、失访的观测概率/HT/Hájek 审计、生成模型与逻辑回归、完全随机化、干扰与单调不依从估计目标反例 | 概率分类与有限状态 MCMC 实验（含 Beta–Binomial 批量预测证书、部分汇聚收缩/方差证书、Markov 滞后协方差/有限方差证书、AR(1) 精确区间/欠覆盖/声明 HAC 重放、重复日志的有效样本量重放、全方差的直接混合重放、有限步混合偏差、右截尾风险集/RMST、两组区间→最小效应人工复核、可重放 Platt 产物→成本决策、相关证据模型比较、已声明组宇宙、观测级/时间块/簇级/时间分层簇级/配对纵向 bootstrap、失访观测概率与加权均值审计、Wilson 分箱区间、PSI 点报告与固定类别宇宙的重采样区间、完全随机化、干扰与不依从证书） | 真实时间序列、失访模型与超参数敏感性分析 |
| 数值计算 | 17 篇 | 浮点表示、绝对/相对误差与消去（含一阶尺度/有限扰动交叉项对照）、算法稳定性、直接法与部分选主元、条件数、Kahan/pairwise 稳定求和（含补偿状态轨迹）、容差与属性测试、受保护 Newton 轨迹→连续性声明下的位置预算评审及局部残差割线合同、迭代线性方程组（含对角占优/谱半径对照）、预条件共轭梯度（含 identity/Jacobi 同问题比较）、数值微分（含中心/前向模板边界）、插值（含等距/Chebyshev 节点的同网格对照）、数值积分（含端点奇性与无界幂尾分界）、多维采样（含精确方差与样本/理论标准误对照）、重要性采样诊断（含对数权重稳定化与适配—估计分离）、随机模拟与可复现性 | 浮点数错误博物馆（含 binary64、补偿求和轨迹、主元、病态系统、求根任务诊断与位置预算评审、Jacobi 谱半径对照、identity/Jacobi PCG 比较、Runge 节点比较、二维积分精确方差/标准误、端点/无界幂尾、对数权重与适配—估计分离证书） | 一般无界黑盒与序贯/多阶段提议的边界 |
| 数论与密码学 | 15 篇 | 快速幂（含逐位轨迹与两种位分支的不变量证明）、扩展欧几里得（含余数链—回代轨迹与递归规格证明）、模逆元、CRT（含受限 RSA-CRT 单分支故障的 gcd 证据）、素性、有限域与群（含指数按子群阶归约见证）、RSA、数字签名（含裸 RSA 可乘反例）、公钥生命周期（含撤销状态目标/授权/新鲜度对照）、已签名更新防回滚、透明日志到根轮换的跨课日志覆盖复核、DH（含确认/身份边界）、密码存储、MAC、椭圆曲线预备、根 ID/独立运营者阈值与紧急恢复兼容复核 | 密码学玩具箱（含有限群阶/指数周期证书、受限 CRT 故障 gcd 重放、裸签名可乘反例、DH 确认对照、发布策略、撤销状态新鲜度重放、Merkle 追加产物→根轮换人工复核、独立运营者及客户端恢复兼容证书） | 真实签名/身份、设备状态与网络恢复治理 |

## 验证口径

1. `npm run content:check`：课程正文结构、元信息、练习数量、答案提示数量、内部链接、路线锚点与专题课程数检查；
2. `npm run projects:test`：所有教学项目的单元测试；
3. `npm run build`：公式、导航、静态站点构建；
4. GitHub Pages：公开页面可访问，数学正文含 KaTeX 输出。
5. 课程路线锚点：内容检查会核对五大专题中每条核心路线均保有对应课程或综合项目页面，避免重命名或删改留下断链。

## 如何使用

- 初学者先沿各专题首页的“课程地图”完成已有深度主线；
- 想进入工程层，优先运行对应项目并完成开放练习；
- 贡献新文章时，复制[深度文章模板](/templates/deep-lesson)，并从本页“下一关键缺口”选择题目。

## 维护原则

不以文章数量作为完成指标。一个专题只有在预备知识、核心概念、推导算法、工程实验四层均形成可阅读路径时，才可称为“深度版完成”。v3.8 Meta Review 后，后续升级还必须证明读者能力的改变：能定位错误路径、跨课迁移概念、比较决策权衡，或主动改变输入并解释输出；不能只为已有结果增加同类报告字段。
