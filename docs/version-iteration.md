---
title: 版本迭代看板
description: 用可验证的目标、范围、增量与质量门禁持续推进计算机数学课程库。
search: false
---

# 版本迭代看板

这个项目以 Markdown 文章和可运行实验为主体。版本不是文章数量的计数器：只有当读者多获得一条完整、可复跑、可检查的学习路径时，才创建一个版本增量。

## 当前基线：v1.0

当前基线已覆盖五个专题的 75 篇课程文章；每篇课程有学习层级、前置知识、预计时长、至少四道练习及答案提示。核心主题同时要求问题场景、定义或直觉、推导/算法、失败边界和可运行验证。具体覆盖与尚未解决的缺口见[课程成熟度看板](/course-maturity)。

基线质量门禁如下：

```bash
npm run content:check
npm run projects:test
npm run build
```

发布后还需确认 GitHub Pages 中导航可达、内部链接有效且公式由 KaTeX 正常渲染。

## 已完成：v1.1「可信输出」

### 版本问题

现有课程已经能帮助读者推导算法并运行小实验；下一步要回答更接近工程的问题：一个数值或概率输出何时值得相信，输入或计算条件变化时又该如何识别失效。

### 版本范围

| 路线 | 要交付的学习能力 | 计划产物 |
| --- | --- | --- |
| 概率可信性 | 区分排序能力与概率校准，能在独立数据上重新校准模型 | 再校准课程、前后指标对比、可靠性报告 |
| 漂移监控 | 区分协变量、标签与概念漂移；理解告警不是因果结论 | 漂移课程、固定样例、分箱统计和阈值报告 |
| 数值可信性 | 区分残差、后向误差、前向误差与条件数 | 直接法课程、病态系统可视化与可复跑对照 |

### 不在本版本范围内

- 为了数量而扩展新的密码协议或大规模算法专题；
- 与教学闭环无关的全站视觉重构；
- 没有数据契约、反例或测试支撑的“结论型”文章。

## 增量的完成定义

每一个 v1.1 增量都必须同时满足：

1. **课程闭环**：从问题场景到推导，再到失败边界、练习与后续链接；
2. **实验闭环**：输入、参数、随机种子（如适用）、输出指标和结论均可复跑；
3. **证据闭环**：至少有一个应当失败的反例、相应的自动化测试或独立重放检查；
4. **站点闭环**：文章已进入专题导航，并通过全部构建门禁。

## 迭代记录格式

后续版本提交在提交信息和变更说明中记录以下字段：

| 字段 | 要回答的问题 |
| --- | --- |
| 目标 | 读者新获得什么可验证能力？ |
| 范围 | 哪些文章、实验、测试与导航受影响？ |
| 验证 | 哪些命令、报告或页面证明它完成？ |
| 差异 | 相比上一基线具体增加或修正了什么？ |
| 下一决策 | 继续同一条学习链，还是回到[成熟度看板](/course-maturity)选择新的关键缺口？ |

若一个改动不能明确回答这些问题，就先作为草稿或问题记录，而不冒充一次版本发布。

## v1.1 增量记录

### v1.1-a：验证集再校准（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能将模型分数和概率承诺区分开，并避免用最终测试集拟合校准器。 |
| 范围 | 新增[概率再校准](/probability-ml/recalibration)课程、`PlattCalibrator`、概率损失工具与单元测试；更新概率专题导航、项目页和成熟度看板。 |
| 验证 | `python -m unittest projects.naive_bayes_spam.test_recalibration`、`npm run projects:test`、`npm run content:check`。 |
| 差异 | 原有可靠性分箱只能诊断概率；现在可在显式验证集上拟合单调 logistic 后处理，并记录不会增加正则化目标的优化轨迹。 |
| 下一决策 | 在同一条“可信输出”路径上补充数据漂移监控；它应解释何时不能再相信这次校准，而不是自动重训或自动上线。 |

### v1.1-b：类别数据漂移监控（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能用参考期与当前期的类别频率识别输入变化，并明确告警只要求人工审查。 |
| 范围 | 新增[数据漂移监控](/probability-ml/data-drift-monitoring)课程、PSI/总变差报告、独立重放证书与单元测试；更新概率专题导航、项目页和成熟度看板。 |
| 验证 | `python -m unittest projects.naive_bayes_spam.test_drift_monitoring`、`npm run projects:test`、`npm run content:check`。 |
| 差异 | 原项目只说明漂移风险；现在会保留新类别、份额、距离与政策阈值，并拒绝被篡改的结论。 |
| 下一决策 | 转入数值可信性：以直接法和病态系统说明为何小残差不能单独保证解误差小。 |

### v1.1-c：直接法与选主元（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能区分微小主元造成的算法不稳定，与病态问题导致的输入敏感性。 |
| 范围 | 新增[直接法与选主元](/numerical-computing/direct-methods-pivoting)课程、无选主元/部分选主元对比报告、轨迹重放证书与单元测试；更新数值专题导航、项目页和成熟度看板。 |
| 验证 | `python -m unittest projects.floating_point_museum.test_direct_methods`、`npm run projects:test`、`npm run content:check`。 |
| 差异 | 原有条件数课解释“后向误差小而前向误差大”；现在额外演示未选主元会沿计算路径直接损坏消元结果，以及部分选主元如何避免该类可避免误差。 |
| 下一决策 | 为病态系统补充交互式可视化：让读者改变扰动尺度，同时观察解的放大、残差与条件数界。 |

### v1.1-d：病态系统交互可视化（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能亲手改变右端扰动尺度，并同时观察输入相对变化、解放大、条件数界与零残差如何共存。 |
| 范围 | 新增 `ConditioningExplorer`，接入[条件数](/numerical-computing/condition-number)课程和[浮点数错误博物馆](/projects/floating-point-museum)。 |
| 验证 | `npm run build`；本地课程页加载、滑动 ε、桌面与移动端渲染、控制台健康检查。 |
| 差异 | 原有 Python 报告需要读者运行；现在浏览器内可立即比较扰动尺度与放大关系，并明确柱状图仅为可读性缩放。 |
| 下一决策 | 完成 v1.1 的数值可信性收束审计，再决定是否进入新的专题缺口，而不因持续迭代而人为扩张范围。 |

## v1.1 收束审计

| 完成条件 | 当前证据 | 结论 |
| --- | --- | --- |
| 概率可信性 | [概率再校准](/probability-ml/recalibration)课程、`PlattCalibrator`、验证/保留集边界、损失与优化轨迹测试 | 已完成 |
| 漂移监控 | [数据漂移监控](/probability-ml/data-drift-monitoring)课程、PSI/总变差报告、新类别与篡改证书测试 | 已完成 |
| 数值可信性 | [直接法与选主元](/numerical-computing/direct-methods-pivoting)、[条件数](/numerical-computing/condition-number)与浏览器内交互面板 | 已完成 |
| 质量与发布 | `npm run build` 通过：83 篇正文、295 项测试；[GitHub Pages 工作流](https://github.com/xiaobiG/computer-mathematics/actions/runs/33293460927)成功 | 已完成 |

审计结论：v1.1 已实现“概率输出需在独立验证集审计、输入变化需触发人工审查、数值残差需结合条件数解释”的完整学习链。它不声称任何教学分类器或数值示例可替代生产环境的风险管理。

## 已完成：v1.2「图算法对照实验室」

### 版本问题

离散数学已有 BFS、Dijkstra、Bellman–Ford 与 Floyd–Warshall 的独立课程和证书，但读者仍难在同一张图、同一条轨迹上判断各算法的适用边界与计算代价。

### 版本范围

| 路线 | 要交付的学习能力 | 计划产物 |
| --- | --- | --- |
| 同图对照 | 在无权、非负权、含负边、负环四类图上选择正确算法 | 可编辑小图、算法结果/轨迹并列报告 |
| 不变量对照 | 区分 BFS 分层、Dijkstra 定型、Bellman–Ford 按边数松弛、Floyd 中间点集合 | 同一输入下的状态解释和可重放证书 |
| 复杂度边界 | 用顶点数、边数、源点数和稠密度选择算法 | 小规模实测与理论复杂度报告 |

### 不在本版本范围内

- 把教学图编辑器包装成通用图数据库或生产路径规划器；
- 引入没有可复跑验证的动画；
- 脱离既有 BFS、Dijkstra、Bellman–Ford、Floyd–Warshall 学习路径的随机新主题。

## v1.2 增量记录

### v1.2-a：同图最短路算法对照（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能在四类图上先核对算法前提，再解释路径、距离或拒绝结论。 |
| 范围 | 新增[最短路算法选择](/discrete-math/shortest-path-algorithm-selection)课程、统一 Python 对照报告/证书，以及浏览器内同图对照面板。 |
| 验证 | `python -m unittest projects.algorithm_lab.test_shortest_path_comparison`、`npm run build`、负边/负环案例的浏览器交互验证。 |
| 差异 | 原项目仅分别展示轨迹；现在同一边表能并列审计 BFS、Dijkstra、Bellman–Ford 和 Floyd–Warshall 的适用性与拒绝理由。 |
| 下一决策 | 在不扩展到通用图数据库的前提下，为 v1.2-b 设计受限的小图输入契约与可重放报告。 |

### v1.2-b：受限小图输入与可重放报告（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能输入自己的小图，同时保留可检查的边界与可复制的重放锚点。 |
| 范围 | `shortest-path-comparison/v1` 输入合同、规范化/JSON 重放报告/证书及其单元测试；最短路对照面板的自定义编辑器、输入 JSON 复制与报告预览；课程、实验室与成熟度看板同步。 |
| 验证 | `python -m unittest projects.algorithm_lab.test_shortest_path_comparison`、`npm run build`、浏览器中有效输入、超限输入和移动端交互检查。 |
| 差异 | v1.2-a 只能切换固定图；现在每次自定义运行都绑定顶点、边、源点与目标的版本化 JSON，并可在 Python 中独立重放和拒绝篡改。 |
| 下一决策 | 比较小图在不同稠密度、查询数量下的理论复杂度与可重复实测，补上“适用”之后的工程成本判断。 |

### v1.2-c：查询数量与可重放工作量（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能区分一次单源查询与多次查询，并在不依赖机器时钟的情况下比较工作量。 |
| 范围 | 新增 `shortest_path_workload_report` 与重放证书；它记录实际边扫描、成功松弛、Bellman–Ford 轮次和 Floyd–Warshall 候选格。对照面板新增查询数量滑块及理论单位卡片；课程和实验室说明同步。 |
| 验证 | `python -m unittest projects.algorithm_lab.test_shortest_path_workload`、`npm run build`、浏览器中改变查询数并检查适用性与工作量卡片。 |
| 差异 | v1.2-b 只让读者重放一份最短路结论；现在可将同一份输入扩展为多个源点查询，并审计“重复单源”与“一次全源预处理”的成本口径。 |
| 下一决策 | 继续检查密度、内存预算和“找到目标即可停止”的查询语义，防止将完整单源工作量误用于目标导向任务。 |

### v1.2-d：目标查询、密度与存储边界（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能区分完整单源运行与固定目标查询，知道“停止”本身也需要算法不变量证明。 |
| 范围 | 新增 `shortest_path_query_boundary_report` 与重放证书，记录 BFS/Dijkstra 的安全目标停止、Bellman–Ford/Floyd–Warshall 的不能提前停止边界，以及有向密度、邻接表槽位和矩阵格；对照面板加入相应事实卡片。 |
| 验证 | `python -m unittest projects.algorithm_lab.test_shortest_path_query_boundary`、`npm run build`、浏览器中单位图/负边图与移动端交互检查。 |
| 差异 | v1.2-c 比较的是完整单源查询次数；现在报告明确何时可以少扫描边、何时不能把“暂时到达目标”误当作最终最短路，并把稀疏表示与全源矩阵的空间代价放入同一证据链。 |
| 下一决策 | 审计输入图被更新后，已有最短路、工作量和边界报告应如何失效、重放和比较，避免旧结论被静默复用。 |

### v1.2-e：图更新与报告失效审计（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能在边表更新后区分“输出仍相同”与“旧证据仍有效”，并知道何时必须重放。 |
| 范围 | 新增 `shortest_path_update_report` 与证书：以规范化 JSON 的 SHA-256 指纹绑定前后图，比较多重集边差异和算法输出变化，并使旧比较、工作量、查询边界报告失效；浏览器面板加入基线快照与更新影响展示。 |
| 验证 | `python -m unittest projects.algorithm_lab.test_shortest_path_update`、`npm run build`、浏览器中固定基线、修改自定义边表并检查失效状态。 |
| 差异 | v1.2-d 只对一份静态输入报告停止和存储边界；现在输入更新得到显式的“旧证据失效 / 新图重放”结论，且不会因为路径恰好相同而漏掉变化。 |
| 下一决策 | 对 v1.2 的同图选择、受限输入、工作量、目标停止和更新审计做收束检查，再从成熟度看板选取下一条真正高价值的学习链。 |

## v1.2 收束审计

| 完成条件 | 当前证据 | 结论 |
| --- | --- | --- |
| 同图算法选择 | [最短路算法选择](/discrete-math/shortest-path-algorithm-selection)课程、`ShortestPathComparisonExplorer`、`shortest_path_comparison` 与拒绝卡证书 | 已完成 |
| 受限输入与重放 | `shortest-path-comparison/v1` 合同、浏览器小图编辑器、JSON 输入复制、重放与篡改证书 | 已完成 |
| 多查询成本 | `shortest_path_workload_report` 对实际边扫描/松弛/矩阵候选格计数，查询数量交互卡片 | 已完成 |
| 目标查询与存储 | `shortest_path_query_boundary_report` 对安全停止、不可提前停止、密度和表示空间的证据 | 已完成 |
| 更新失效 | `shortest_path_update_report` 的输入指纹、多重集边差异及旧报告失效结论 | 已完成 |
| 端到端一致性 | `test_shortest_path_learning_chain` 以同一合同联测五条证据链；全项目构建与桌面/移动端回归 | 已完成 |

审计结论：v1.2 已使读者能够从“这张图能否使用某算法”推进到“输入、查询语义、工作量、存储与图更新变化后，哪份结论仍可被证明”。它不试图提供生产级动态最短路服务，也不把教学小图的计数外推为现实系统吞吐量。

## 正在推进：v1.3「带标签窗口下的性能退化审计」

### 版本问题

现有概率专题可用 PSI/总变差发现输入漂移，也可做概率校准；但标签往往延迟到达。读者还缺少一条可重放学习链：当一个带标签窗口到达时，如何区分输入变化、分类性能退化和概率承诺失真，并声明样本量与结论边界。

### 版本范围

| 路线 | 要交付的学习能力 | 计划产物 |
| --- | --- | --- |
| 带标签窗口合同 | 固定参考/当前窗口、类别、预测分数和延迟标签的口径 | 规范化输入、重放报告与篡改证书 |
| 性能退化 | 同时审计错误率、对数损失、混淆矩阵和不确定性 | 课程、可运行项目、浏览器内窗口对照 |
| 决策边界 | 区分“统计信号”“需要复核”和“自动行动” | 阈值说明、反例和测试 |

### 不在本版本范围内

- 自动重训、自动部署或任何生产风险决策；
- 将有限教学窗口的统计量解释为因果结论；
- 没有独立重放与失败反例支撑的性能声明。

## v1.3 增量记录

### v1.3-a：带标签窗口性能退化审计（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能在延迟标签到达后，分开审计阈值分类质量与概率承诺，并知道信号只要求人工复核。 |
| 范围 | 新增[带标签窗口性能审计](/probability-ml/labeled-window-performance-degradation)课程、`labeled-window-monitoring/v1` 合同、报告/篡改证书与单元测试；新增浏览器内窗口对照面板；更新专题导航、项目页与成熟度看板。 |
| 验证 | `python -m unittest projects.naive_bayes_spam.test_labeled_window_monitoring`、全项目测试与内容检查、生产构建，以及稳定/准确率下降/过度自信三种浏览器情形的桌面和移动端检查。 |
| 差异 | 过去只能检查输入 PSI 或单个静态校准结果；现在参考与当前的带标签窗口被同一合同绑定，混淆矩阵、准确率 Wilson 区间、Brier、对数损失、政策阈值与 `automatic_action: none` 可独立重放。 |
| 下一决策 | v1.3-b 应将输入漂移证据与带标签窗口证据并列，而不把二者的同时出现误写为因果关系或自动行动。 |

### v1.3-b：同窗口联合证据审计（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能将输入类别变化与带标签性能变化放入同一份可重放报告，同时明确二者并列不等于因果。 |
| 范围 | 新增[联合证据](/probability-ml/joint-input-label-evidence)课程、`joint-evidence-monitoring/v1` 快照合同、联合报告/证书及单元测试；更新专题导航与成熟度看板。 |
| 验证 | `python -m unittest projects.naive_bayes_spam.test_joint_evidence_monitoring`、全项目测试、内容检查与生产构建。 |
| 差异 | v1.3-a 只能并排阅读两份独立报告；现在类别、概率和标签以等长快照绑定，报告可分别指出输入与结果信号，并拒绝篡改为因果解释或自动行动。 |
| 下一决策 | 转向分层子群体下的校准与性能不确定性；先定义样本量与隐私边界，避免以极小群体的噪声生成结论。 |

### v1.3-c：子群体样本量边界（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能在解释子群体指标前先检查样本量，并将样本不足明确报告为证据不足。 |
| 范围 | 新增[子群体性能](/probability-ml/subgroup-performance-uncertainty)课程、`subgroup-monitoring/v1` 报告/证书和反例测试；接入专题导航与成熟度看板。 |
| 验证 | `python -m unittest projects.naive_bayes_spam.test_subgroup_monitoring`、内容检查、全项目测试和生产构建。 |
| 差异 | 总体窗口可能掩盖局部风险，也可能诱导极小分组比较；现在政策阈值被绑定进证书，低样本组拒绝生成指标结论。 |
| 下一决策 | 对 v1.3 概率监控链做收束审计，确认输入、标签、联合证据与子群体边界在同一项目中可共同解释。 |

## v1.3 收束审计

| 完成条件 | 当前证据 | 结论 |
| --- | --- | --- |
| 带标签性能 | 总体窗口合同、准确率 Wilson 区间、Brier/对数损失与篡改证书 | 已完成 |
| 输入—标签联合证据 | 同一观测快照、PSI 与结果信号并列、因果结论拒绝 | 已完成 |
| 子群体边界 | 最低样本量合同、小样本拒绝和证书 | 已完成 |
| 端到端一致性 | `test_monitoring_learning_chain` 用同一窗口联测三条证据链；全项目构建门禁 | 已完成 |

审计结论：v1.3 已将“输入变化”“带标签性能变化”“同窗口联合证据”和“子群体样本量边界”组织为一条可重放学习链。它不提供因果归因、群体排序、自动重训或自动部署；后续应从成熟度看板重新选择高价值缺口。

## 已完成：v1.4「随机低秩分解的可复现证据」

### v1.4-a：随机 SVD 小矩阵分解（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能在随机范围发现后分解小矩阵 $B=Q^TA$，并将截断重构、误差与种子一起重放。 |
| 范围 | 新增[随机 SVD](/linear-algebra/randomized-svd)课程、`randomized_svd_report`/证书、确定性与篡改测试；接入线性代数首页、侧栏和实验室页面。 |
| 验证 | `python -m unittest projects.linear_algebra_lab.test_randomized_svd`、全项目测试、内容检查、生产构建及 GitHub Pages 远程构建。 |
| 差异 | 原随机范围发现止于 $QQ^TA$；现在将该子空间中的小型分解映射回原空间，并区分一次随机近似误差与精确截断 SVD 的最优误差。 |
| 修复记录 | 首次远程发布暴露 NumPy 不存在；实现已改为复用项目内的有限迭代教学 SVD，保持零额外 Python 依赖。 |
| 下一决策 | 先对 v1.4 的随机草图、分解、证书和发布环境做收束检查，再从成熟度看板选择下一条学习链。 |

## 正在推进：v1.5「分层概率承诺的审计」

### 版本问题

总体可靠性图、Brier 分数和子群体性能报告已经存在，但读者仍可能把“总体 ECE 很低”误读为每个充分样本子群的概率都可信。下一步要把这种平均抵消明确变成可运行、可重放的反例与报告合同。

### v1.5-a：总体平均与分组校准反例（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能用固定分箱 ECE 发现总体校准掩盖的局部概率偏差，并拒绝将描述性信号写成因果、公平性或自动行动结论。 |
| 范围 | 新增[分组校准](/probability-ml/subgroup-calibration-uncertainty)课程、`subgroup-calibration/v1` 合同、分箱/ECE 报告、重放证书与反例测试；同步概率导航、项目页、成熟度看板和实际课程计数。 |
| 验证 | `python -m unittest projects.naive_bayes_spam.test_subgroup_calibration`、`npm run projects:test`、`npm run content:check`、生产构建与 GitHub Pages。 |
| 差异 | v1.3-c 只在样本充足时报告每组性能；现在总体 ECE 为零也不能掩盖两个合格组各为 0.2 的校准偏差，且分箱数、样本门槛与复核阈值均绑定进证书。 |
| 下一决策 | 为合格组的分箱频率加入不确定性区间或重采样边界；保持组预定义、最小样本量、人工复核和不作因果归因的合同。 |

### v1.5-b：分箱频率的不确定性边界（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能区分“观测到的分箱正例率不确定”与“ECE 或未来模型校准不确定”，避免把一个区间过度外推。 |
| 范围 | `subgroup-calibration/v1` 在每个非空分箱新增 Wilson 正例率区间及映射的绝对差范围；证书绑定 `confidence_z`；课程与项目页补充公式、反例、边界和练习。 |
| 验证 | `python -m unittest projects.naive_bayes_spam.test_subgroup_calibration`、`npm run projects:test`、`npm run content:check`、生产构建与 GitHub Pages。 |
| 差异 | v1.5-a 只能报告观察到的 ECE；现在读者可查看每箱频率可能范围，并明确 ECE 不是将这些端点直接相加得到的区间。 |
| 下一决策 | 若继续该链，应先定义冻结窗口与预先注册的比较目标，再研究跨窗口的校准差异；不以事后选组、选箱或自动决策扩张报告。 |

## 正在推进：v1.6「数值预算的可重放边界」

### v1.6-a：自适应积分的函数调用预算（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能区分“估计误差预算”与“函数调用资源预算”，并在预算耗尽时得到可审计的未收敛结果。 |
| 范围 | 扩展自适应 Simpson 报告，新增最大调用数、终止叶区间、预算耗尽状态和独立重放证书；补充课程、实验室页与成熟度看板。 |
| 验证 | `python -m unittest projects.floating_point_museum.test_integration`、全项目测试、内容检查、生产构建与 GitHub Pages。 |
| 差异 | 原实现仅以深度边界拒绝不充分的误差估计；现在先预检每次细分所需的两个新点，调用预算耗尽时保留父区间、标记误差未知，并拒绝篡改的叶轨迹。 |
| 下一决策 | 不再重复一维自适应 Simpson；下一步应从成熟度看板选择多维结构化采样，或对当前数值实验做跨模块收束审计。 |

## 正在推进：v1.7「图更新后的受限修复」

### v1.7-a：单边插入的增量最短路（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能区分旧图证据失效与新图从零计算，理解何时一条插入边只需局部松弛传播。 |
| 范围 | 新增[增量最短路](/discrete-math/incremental-shortest-path)课程、`incremental-shortest-path/v1` 报告/证书及反例测试；同步离散数学路线、侧栏、算法实验室、成熟度看板和课程规模。 |
| 验证 | `python -m unittest projects.algorithm_lab.test_incremental_shortest_path`、`npm run projects:test`、`npm run content:check`、生产构建与 GitHub Pages。 |
| 差异 | v1.2-e 能证明图更新令旧报告失效；现在在顶点/查询不变、恰好一条非负边插入的合同下，复用旧距离从下降端点局部 Dijkstra 松弛，并逐点比对完整重算。 |
| 下一决策 | 删除、改权、负边和批量更新必须维持独立语义；下一轮应优先建立替换路径或批量更新的专门合同，而非扩大此插入算法的适用声明。 |

## 正在推进：v1.8「数学对象与内存坐标」

### 版本问题

现有预备知识已经说明符号、函数和有限求和，却还没有把“元素属于集合”与“值位于数组某个位置”作为两个不同的程序契约讲清楚。这个缺口会直接放大后续向量、矩阵和图存储的理解成本。

### v1.8-a：集合与数组形状可视化（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能区分成员关系与零基索引，并可重放有限集合的并、交、差及二维数组的形状、坐标和值。 |
| 范围 | 新增[集合与数组形状](/foundations/sets-array-shapes)课程、站内交互探索器、`sets-arrays/v1` 报告/证书与反例测试；同步预备知识导航、实验室页和成熟度看板。 |
| 验证 | 模块单元测试、全项目测试、内容检查、生产构建；桌面及 390px 移动端验证集合操作、网格选中和无横向页面溢出。 |
| 差异 | 先前只能从求和的循环索引间接理解位置；现在集合重复、非矩形行和越界坐标都会在独立合同中被拒绝，网页可直接观察“交集”与“坐标取值”的不同语义。 |
| 下一决策 | 以形状契约为共同前置，补齐标量、向量、矩阵与批处理维度之间的转换；保持小尺寸、零依赖和可重放边界。 |

### v1.8-b：张量形状与行批次合同（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能将标量、向量、列矩阵与行批次区分为不同接口，推导批量线性层的输入、输出和偏置形状。 |
| 范围 | 新增[张量形状与批处理](/foundations/tensor-shapes-batches)课程、形状追踪器、`tensor-shapes/v1` 报告/证书及反例测试；同步预备知识导航、实验室页和成熟度看板。 |
| 验证 | 模块单元测试、全项目测试、内容检查、生产构建；浏览器中切换逐元素加法、矩阵乘法、批量线性层，并检查窄屏布局。 |
| 差异 | v1.8-a 只保证二维数组的矩形坐标；现在 `()`、`(n,)`、`(n,1)` 与 `(batch,features)` 均有独立合同，`X @ W + c` 同时绑定数值、轴语义和形状。 |
| 下一决策 | 预备知识的对象—轴—批次链已贯通到矩阵乘法；下一轮应将集合与图的不同存储表示（邻接表、邻接矩阵）连到时间/空间权衡。 |

## 正在推进：v1.9「结构表示与算法工作量」

### v1.9-a：邻接表与邻接矩阵的可重放对照（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能将同一无权有限图构造为邻接表和邻接矩阵，并按查边、枚举邻居、方向和存储选择表示。 |
| 范围 | 新增[图的两种存储](/discrete-math/graph-representations)课程、`graph-representations/v1` 双表示报告/证书与反例测试；同步离散数学路线、算法实验室、导航和成熟度看板。 |
| 验证 | 模块单元测试、全项目测试、内容检查、生产构建与 GitHub Pages。 |
| 差异 | 先前图课程仅以文字说明稀疏/稠密取舍；现在同一边表被同时重建为两种内存对象，每次查询记录列表检查数、矩阵单元访问和两个答案的一致性。 |
| 下一决策 | 不将 0/1 静态图合同扩张到加权、多重边或动态图；下一轮优先建立“表示变化后算法工作量如何变化”的小规模基准，或从成熟度看板选择替换路径。 |

### v1.9-b：同图 BFS 的表示工作量（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能区分静态存储成本与一次搜索的实际扫描数，并验证两种表示在同一 BFS 上保留相同距离。 |
| 范围 | 扩展 `graph-representations/v1`，新增双 BFS 重放报告/证书和反例测试；课程补充可运行实验与可达子图边界。 |
| 验证 | 模块单元测试、全项目测试、内容检查、生产构建与 GitHub Pages。 |
| 差异 | v1.9-a 仅报告每行的潜在成本；现在从给定起点实际计数已访问列表槽位或矩阵格，并将结果绑定到相同的距离与出队顺序。 |
| 下一决策 | 保持静态、无权、简单图合同；下一轮从成熟度看板重新选择高价值缺口，不将小图操作计数包装为通用硬件基准。 |

### v1.9-c：KaTeX 否定关系的构建稳定性（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 消除生产构建中否定成员关系的字形度量警告，保证集合公式在静态渲染路径上使用稳定的 KaTeX 组合。 |
| 范围 | 将集合课程中的预组合 `\notin` 输出改为显式 `\not\in` 组合；不改变数学含义或课程接口。 |
| 验证 | 内容检查、346 项项目测试与生产构建；构建日志不再出现 `No character metrics for '∉'`。 |
| 差异 | v1.9-b 后站点虽可构建但仍伴随三次公式字体警告；现在 KaTeX 日志无该警告，排除关系由渲染器的基础符号组合完成。 |
| 下一决策 | 完成表示链的收束检查后，从成熟度看板选择下一个专题级缺口；对每个新公式继续把无警告构建作为发布门禁。 |

## 正在推进：v2.0「多维近似的采样预算」

### v2.0-a：二维网格与固定种子蒙特卡洛（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能在同一真值已知的二维积分中比较张量网格的指数预算增长与固定种子蒙特卡洛的标准误解释。 |
| 范围 | 新增[多维积分的结构化采样](/numerical-computing/multidimensional-integration)课程、`unit-square-integration/v1` 报告/证书及反例测试；同步数值专题、博物馆页和成熟度看板。 |
| 验证 | 模块单元测试、全项目测试、内容检查、生产构建与 GitHub Pages。 |
| 差异 | 原数值积分只覆盖一维复合/自适应规则，随机模拟只估计圆周率；现在同一二维函数由中点网格和蒙特卡洛共同重放，分别绑定样本预算、误差和标准误。 |
| 下一决策 | 不将该光滑单位正方形基准扩张为奇点、无界区域或通用高维求积；下一轮应从成熟度看板重新选择真正高价值的专题缺口。 |

## 正在推进：v2.1「冻结窗口的概率承诺」

### v2.1-a：跨窗口校准差异的预先定义合同（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能在看指标前冻结参考/当前窗口、分箱和最低样本政策，再将校准差异限制为人工复核证据。 |
| 范围 | 新增[冻结窗口的校准比较](/probability-ml/frozen-window-calibration-comparison)课程、`window-calibration-comparison/v1` 报告/证书与反例测试；同步概率路线、项目页和成熟度看板。 |
| 验证 | 模块单元测试、全项目测试、内容检查、生产构建与 GitHub Pages。 |
| 差异 | 原分组校准只在单个窗口内审计偏差；现在两个预先命名、充分样本的窗口在相同分箱政策下比较 ECE/Brier 差，并拒绝事后改阈值、因果归因和自动行动。 |
| 下一决策 | 若继续此链，应先预先指定重采样或区间设计来描述差异的不确定性；不得从一个固定差值跳到显著性或部署结论。 |

## 路线复盘：v2.2「避免重复覆盖」

审计密码学路线后确认：现有 [RSA](/number-theory-crypto/rsa) 已明确裸 RSA 的确定性/可乘性、OAEP/PSS、密码学安全随机数、常量时间、故障攻击和密钥生命周期边界；[数字签名](/number-theory-crypto/digital-signatures)也已拒绝自制填充并指向 Ed25519/RSA-PSS 与上下文绑定。成熟度看板先前的“标准签名填充与侧信道”因此不再是实际缺口，已改为公钥身份绑定、轮换与撤销的协议链。

这次复盘不创建重复课程或自制密码学代码；下一版本应围绕可信公钥分发、密钥标识、轮换、撤销和验签上下文，且仍不得将教学玩具箱扩展为生产密码方案。

## 正在推进：v2.2「公钥身份与生命周期」

### v2.2-a：验签前的身份、轮换与撤销状态（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能区分“某公钥验签成功”和“预期身份的公钥获准用于此协议”，并按顺序审计信任链、用途、时间、撤销和上下文。 |
| 范围 | 新增[公钥身份与密钥生命周期](/number-theory-crypto/public-key-lifecycle)课程；同步密码学专题导航、项目页、成熟度看板与实际课程计数。 |
| 验证 | `npm run content:check`、`npm run projects:test`、`npm run build`，以及 GitHub Pages 远程构建。 |
| 差异 | v2.2 路线复盘只指出缺口；现在把公钥替换、跨协议重放、撤销优先级、轮换重叠期和撤销信息不可用显式组织为一条课程链。 |
| 下一决策 | 在不自制证书、撤销或签名实现的前提下，评估“防回滚”和“撤销信息可用性”在不同风险策略下的边界。 |

### v2.2-b：有效签名后的版本状态审计（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能证明一份已签名的旧发布仍应被拒绝，并明确撤销信息缺失不可以静默转为接受。 |
| 范围 | 新增[已签名更新为何仍可能回滚](/number-theory-crypto/signed-release-anti-rollback)课程、`signed-release-policy-audit/v1` 虚构发布元数据报告/证书与反例测试；同步专题导航、项目页与成熟度看板。 |
| 验证 | `python -m unittest projects.crypto_toybox.test_release_policy`、`npm run content:check`、`npm run projects:test`、`npm run build` 与 GitHub Pages。 |
| 差异 | v2.2-a 描述公钥与撤销状态；现在候选版本与受保护的最低版本被绑定在可重放合同中，数学验签声明为真也不能绕过防回滚、身份、上下文或撤销检查。 |
| 下一决策 | 评估可信信任锚如何更新及透明日志如何提供可审计性；继续坚持不实现真实签名、证书解析或更新安装。 |

### v2.2-c：透明日志的成员与追加审计（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能区分 Merkle 成员证明、完整列表的追加前缀审计与真实信任锚更新。 |
| 范围 | 新增[透明日志的 Merkle 证明](/number-theory-crypto/transparency-logs)课程、`append-only-merkle-log/v1` 的 SHA-256 成员证明/追加报告/证书与反例测试；同步导航、项目页与成熟度看板。 |
| 验证 | `python -m unittest projects.crypto_toybox.test_transparency_log`、全项目测试、内容检查、生产构建与 GitHub Pages。 |
| 差异 | v2.2-b 只在候选发布记录上审计状态；现在可区分日志的新增与历史重写，并把“根哈希不能认证信任锚”编码为不可被证书篡改的边界。 |
| 下一决策 | v2.2 的身份—撤销—防回滚—可审计历史链已闭合；应进行路线收束审计，再从成熟度看板选择不同专题的关键缺口。 |

## 正在推进：v2.3「从数值误差到结构质量」

### v2.3-a：全局 SSIM 的可重放对照（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能将逐像素 MSE/PSNR 与由均值、方差、协方差构成的结构相似度区分开。 |
| 范围 | 新增[结构相似度 SSIM](/linear-algebra/structural-similarity)课程，扩展图像指标实验室的全局 SSIM 报告与证书，并同步导航和成熟度看板。 |
| 验证 | `python -m unittest projects.linear_algebra_lab.test_image_metrics`、全项目测试、内容检查和生产构建。 |
| 差异 | 原实验只度量像素残差；现在可重放亮度、方差、协方差和 SSIM，并以列反转反例说明相同统计层级之外的结构变化。 |
| 下一决策 | 不把全局 SSIM 冒充感知模型；下一步应审计局部窗口、颜色空间和下游任务之间的边界。 |

## 正在推进：v2.4「概率差异的重采样边界」

### v2.4-a：冻结窗口 ECE 差的固定 bootstrap（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能为预先定义的两个窗口 ECE 差描述固定重采样设计下的百分位区间。 |
| 范围 | 新增[冻结窗口差异的 Bootstrap](/probability-ml/frozen-window-bootstrap)课程、`window-calibration-bootstrap/v1` 报告/证书及反例测试；同步概率路线与成熟度看板。 |
| 验证 | `python -m unittest projects.naive_bayes_spam.test_window_calibration_bootstrap`、全项目测试、内容检查与生产构建。 |
| 差异 | 原比较只给 ECE/Brier 点差；现在报告将重复次数、种子、置信水平与观测级重采样单位绑定，篡改端点或种子会被拒绝。 |
| 下一决策 | 不将观测级 bootstrap 推广到时间相关或用户簇数据；下一轮应先为 block/cluster 重采样建立独立合同，或切换专题。 |

## 正在推进：v2.5「关系到图可达性的共同语言」

### v2.5-a / v2.5-b：有限关系、邻接矩阵与可达闭包（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能将有序对集合编码为矩阵，并用逐中间点不变量从直接关系推导可达闭包。 |
| 范围 | 新增[有限关系与邻接矩阵](/foundations/finite-relations-matrices)和[关系复合与可达闭包](/foundations/relation-composition-reachability)；扩展预备实验室的关系性质、闭包轨迹与重放证书。 |
| 验证 | `python -m unittest projects.foundations_lab.test_relations`、全项目测试、内容检查、生产构建与 GitHub Pages。 |
| 差异 | 原预备知识只区分集合与数组；现在有序对、矩阵格、传递性和图可达性在同一合同中可检查，后续 BFS/Floyd–Warshall 不再突然引入表示语义。 |
| 下一决策 | 预备路线已足以衔接图课程；不要机械扩展闭包算法，应转向其他专题的真实证据缺口。 |

## 正在推进：v2.6「随机积分的权重诊断」

### v2.6-a：重要性采样的 ESS 与数值边界（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能把提议分布、权重、估计标准误和 ESS 放入同一可重放报告，而不把任一指标误作误差保证。 |
| 范围 | 新增[重要性采样诊断](/numerical-computing/importance-sampling-diagnostics)、`importance-sampling-x8/v1` 基准/证书及反例测试；同步数值路线与浮点数博物馆。 |
| 验证 | `python -m unittest projects.floating_point_museum.test_importance_sampling`、全项目测试、内容检查、生产构建与 GitHub Pages。 |
| 差异 | 既有课程主要给出重要性采样推导；现在同一真值积分可比较均匀与 $q(x)=2x$，并显式处理伪随机精确零点与权重诊断边界。 |
| 下一决策 | 不将一维良性提议推广为高维解法；若继续，必须先定义对数权重、支撑覆盖和提议适配的独立合同。 |

## 元复盘：v2.6「从扩展到深化」

最近的实质增量已覆盖密码协议状态、结构质量、概率不确定性、关系/图桥梁与随机积分诊断。复盘发现：继续仅凭成熟度表的“下一缺口”新增文章，会削弱用户最初提出的“知识点讲解不能太简单”。后续优先级改为：先审计高访问主线文章是否有推导、反例、可运行验证和工程边界；只有确定存在链路断点时才新增专题。每个新版本仍必须保留可重放证据与发布门禁，文章数量不单独构成价值。

## 正在推进：v2.7「行变换到可信求解」

### v2.7-a：高斯消元的不变量证据（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能从初等行变换保持解集的双向论证，连接到消元的三角化不变量，并检查回代解同时满足原系统与最终系统。 |
| 范围 | 深化[高斯消元](/linear-algebra/gaussian-elimination)的等价行变换、主元下消零与回代推导；在线性代数实验室新增 `elimination_invariant_certificate` 及篡改轨迹测试。 |
| 验证 | `python -m unittest projects.linear_algebra_lab.test_main`、`npm run projects:test`、`npm run content:check`、`npm run build` 与 GitHub Pages。 |
| 差异 | 原有轨迹证书只以完整重放接受或拒绝记录；现在将轨迹重放、非零主元、主元下方为零、原系统残差与上三角系统残差分开呈现，读者能定位一条求解记录具体违反了哪项不变量。 |
| 下一决策 | 不把小规模浮点审计冒充秩揭示或生产求解器保证；下一轮先审计最小二乘与消元之间的“精确解不存在”转场是否需要同等深度的反例或证据。 |

### v2.7-b：最小二乘驻点的报告证书（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能从 $A^Tr=0$ 区分“残差较小”与“已达到最小二乘一阶条件”，并审计正规方程与 QR 两条教学路径的报告。 |
| 范围 | 扩展[最小二乘](/linear-algebra/least-squares)课程与线性代数实验室，新增 `least_squares_report_certificate` 和报告篡改测试。 |
| 验证 | `python -m unittest projects.linear_algebra_lab.test_main`、`npm run projects:test`、`npm run content:check`、`npm run build` 与 GitHub Pages。 |
| 差异 | 原报告只提供两组 $A^Tr$ 和残差范数；现在证书同时重放各字段、验证两条路径的驻点条件，并将记录的残差范数绑定到记录的系数。 |
| 下一决策 | 证书仅覆盖列满秩的教学输入，不能把 normal equation 的可重放性误读为数值首选；下一轮应审计病态数据上“QR 优于正规方程”的示例是否需要可观测对照。 |

### v2.7-c：近共线列的双重边界（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能观察正规方程的尺度平方效应，同时避免把 QR 可返回一个驻点误读为系数已可信。 |
| 范围 | 为[最小二乘](/linear-algebra/least-squares)新增 $10^{-7}$ 近共线列实验，并为“正规方程拒绝、QR 驻点仍成立而系数漂移”的路径加入回归测试。 |
| 验证 | `python -m unittest projects.linear_algebra_lab.test_main`、`npm run projects:test`、`npm run content:check`、`npm run build` 与 GitHub Pages。 |
| 差异 | 原正文只用条件数公式说明风险；现在同一已知系数输入展示正规方程如何先触发绝对容差，以及 QR 的小残差仍不能解决列共线引起的不可辨识性。 |
| 下一决策 | 线性系统到最小二乘的主链已具备推导、证书与病态反例；下一轮应回到五专题成熟度审计，不因邻近主题而继续堆叠线性代数。 |

## 正在推进：v2.8「图更新的单调性边界」

### v2.8-a：单边删除的完整重算审计（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能区分插入边只会降低标签、删除边可能令父路径失效并提高距离，且不把完整重算误称为增量加速。 |
| 范围 | 新增[最短路边删除](/discrete-math/deletion-shortest-path)课程、`deletion-shortest-path/v1` 报告/证书与反例测试；同步离散专题导航、算法实验室和成熟度看板。 |
| 验证 | `python -m unittest projects.algorithm_lab.test_deletion_shortest_path`、`npm run projects:test`、`npm run content:check`、`npm run build` 与 GitHub Pages。 |
| 差异 | 既有单边插入合同仅能下降松弛并明确拒绝删除；现在删除一条旧路径边后，旧/新图由独立 Dijkstra 重放，目标距离上升、替代路径和旧路径端点关系均可审计。 |
| 下一决策 | 当前合同仍不解决批量更新、平行边稳定身份或替换路径数据结构；下一轮优先进行跨专题成熟度审计，不把删除重算扩张为未经证明的动态算法。 |

## 正在推进：v2.9「相关观测的重采样单位」

### v2.9-a：冻结时间块的 Bootstrap 合同（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能把观测级重采样与时间块重采样区分开，并把块边界和块间可交换假设视为报告前提。 |
| 范围 | 新增[时间块 Bootstrap](/probability-ml/block-bootstrap-calibration)课程、`block-window-calibration-bootstrap/v1` 报告/证书和篡改测试；同步概率路线、导航与成熟度看板。 |
| 验证 | `python -m unittest projects.naive_bayes_spam.test_block_window_calibration_bootstrap`、`npm run projects:test`、`npm run content:check`、`npm run build` 与 GitHub Pages。 |
| 差异 | 原 bootstrap 仅重抽独立标签观测；现在完整重抽预定义时间块、保留块内记录顺序与样本量，并拒绝单块输入和被篡改报告。 |
| 下一决策 | 时间块不是用户簇或非平稳趋势的通用替身；下一轮应重新审计成熟度缺口，避免仅扩展相邻统计方法。 |

## 元复盘：v2.9「将新深度课纳入回归门禁」

v2.7–v2.9 的共同增量不是新增术语，而是把“可计算的输出”拆成可检查的前提与反例。审计发现新建的删除最短路和时间块 bootstrap 虽已满足通用课程结构，却未被优先深度文章的阶段门禁保护。本次将二者加入 `priorityDeepLessons`：后续编辑必须继续保留问题场景、定义、推导、边界和可运行实验。下一版本不预设专题，先按此门禁审计现有主线是否仍有真实断点。

### v2.9-b：Dijkstra 过程证据闭环（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能区分“最终距离正确”与“展示的堆事件真实发生”，避免把伪造的成功松弛轨迹当成算法证据。 |
| 范围 | 扩展 Dijkstra 最短路证书，重放确定的堆/序号规则并比对每次定型和成功松弛；新增伪造事件拒绝测试，同步课程说明。 |
| 验证 | `python -m unittest projects.algorithm_lab.test_dijkstra_trace`、`npm run projects:test`、`npm run content:check`、`npm run build` 与 GitHub Pages。 |
| 差异 | 原证书可验证标签、父链和边不等式，但未绑定事件的 `relaxed` 字段；现在即使终值正确，篡改过程事件也会使证书失效。 |
| 下一决策 | 继续审计时优先检查“页面所解释的过程字段”是否都受证书覆盖，而不是仅增加更多算法轨迹。 |

### v2.9-c：最大流增广过程证书（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能验证增广路径、瓶颈、累计流和最终最小割属于同一次 Edmonds–Karp 运行，而非只相信终值。 |
| 范围 | 新增 `max_flow_certificate`、增广轨迹和割集合篡改测试；课程补充多最小割边界；将最大流课加入优先深度门禁。 |
| 验证 | `python -m unittest projects.algorithm_lab.test_max_flow`、`npm run projects:test`、`npm run content:check`、`npm run build` 与 GitHub Pages。 |
| 差异 | 原测试只比较流值与一个割容量；现在过程、最终残量源侧和割等式都绑定到确定重放，且不假设最小割唯一。 |
| 下一决策 | 对其他可视化算法继续检查展示字段是否有独立证据，不把单次输出或最终数值当作完整过程证明。 |

### v2.9-d：条件数扰动报告重放（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能独立验证条件数实验中的前向变化、后向残差和扰动界来自同一组矩阵与右端，而不信任报告内自带的布尔标记。 |
| 范围 | 为右端扰动与矩阵扰动报告新增独立重放证书；课程示例改用具名输入，使每个重放参数可见。 |
| 验证 | `python -m unittest projects.floating_point_museum.test_conditioning`、`npm run projects:test`、`npm run content:check`、`npm run build` 与 GitHub Pages。 |
| 差异 | 原报告只在生成时写入 `certificate` 字段；现在篡改条件数界、残差或证书字段都会与重新计算的整份报告不一致。 |
| 下一决策 | 不把 2×2 精确逆的教学重放外推为通用条件数估计；下一轮审计时应优先检查其他报告型实验是否也缺少独立重放入口。 |

### v2.9-e：随机模拟汇总报告重放（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能验证固定种子实验的均值、样本标准差和标准误确由声明的样本数与种子组产生。 |
| 范围 | 新增 `simulation_report_certificate`、均值篡改拒绝测试，并在[随机模拟](/numerical-computing/stochastic-simulation-reproducibility)课程中说明其统计边界。 |
| 验证 | `python -m unittest projects.floating_point_museum.test_simulation`、`npm run projects:test`、`npm run content:check`、`npm run build` 与 GitHub Pages。 |
| 差异 | 原报告只展示固定种子汇总；现在外部报告可被独立重算，且课程明确证书不代表这组伪随机种子具有总体代表性。 |
| 下一决策 | 在新增证书前继续优先审计报告是否真的支撑页面结论；已具备完整证据链的课程不做重复改动。 |

## 正在推进：v3.0「从生成声明到可重放课程证据」

### v3.0-a：二维 PCA 报告独立重放（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能区分“报告生成时写入的证书标记”和“外部报告可由声明输入独立重算”的证据，并验证 PCA 的中心化、投影与重构误差属于同一次运行。 |
| 范围 | 扩展[特征值与 PCA](/linear-algebra/eigenvalues-pca)、二维 PCA 实验与单元测试；新增 `pca_2d_report_certificate`，并将该课纳入优先深度文章门禁。 |
| 验证 | `python -m unittest projects.linear_algebra_lab.test_pca`、`npm run content:check`；发布前仍须运行全项目测试、生产构建与 GitHub Pages 工作流。 |
| 差异 | 原 `certificate` 仅由同一次报告生成过程写入；现在验证器会从二维样本重算均值、协方差、特征对、得分、重构、平方误差和全部标记，篡改数值或 `valid` 均被拒绝。 |
| 下一决策 | 审计 SVD/PCA 与低秩近似链中其他页面解释的过程量是否有独立证据；不把二维教学重放外推为通用高维 PCA 验证或统计代表性保证。 |

### v3.0-b：低秩链公式健康门禁（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者看到的截断 SVD 与 ALS 正则化公式应与正文推导一致，不能因 Markdown 中一处缺失反斜杠或黏连下标而静默改变含义。 |
| 范围 | 修正[低秩图像压缩](/linear-algebra/low-rank-image-compression)的 `\\qquad` 与[低秩推荐](/linear-algebra/low-rank-recommendation)的两处求和记号；内容检查加入针对这两种已发现错误的窄门禁。 |
| 验证 | `npm run content:check`、`npm run projects:test`、`npm run build`；发布后确认 GitHub Pages 工作流。 |
| 差异 | 原构建能通过，但公式会显示文字 `qquad`，或把正则项的求和下标与因子错误粘连；现在正文修正且同类回归在构建前失败。 |
| 下一决策 | 不把两个模式检查伪装成通用 LaTeX 校验；继续以已渲染核心页与真实学习链审计来发现问题，必要时再扩展有明确语义的门禁。 |

### v3.0-c：全站 KaTeX 预解析（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 在发布前发现会让读者看到错误标记或原始 TeX 的公式，而不是等到公开页面出现问题后再人工定位。 |
| 范围 | 新增 `scripts/check-math.mjs` 并纳入 `content:check`；修复[循环不变量](/discrete-math/loop-invariants)中数学块内的中文句号，以及[深度文章模板](/templates/deep-lesson)中无法解析的公式占位符。 |
| 验证 | `npm run content:check` 对全部 Markdown 公式执行 KaTeX 严格解析；随后运行 `npm run projects:test` 与 `npm run build`。 |
| 差异 | 原有构建会检查页面结构，但不逐式报告 KaTeX 解析错误；现在检查排除代码样例、区分行内/块级公式、定位未闭合定界符与解析异常到页面行号。 |
| 下一决策 | 该门禁只验证语法与可渲染性，不声称验证数学正确性或响应式版式；后续核心页仍应在真实渲染环境中进行针对性审计。 |

### v3.0-d：随机 SVD 完整矩阵重放（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能确认随机 SVD 证书验证的是整份近似矩阵，而非恰好与预期报告相同的一个行列前缀。 |
| 范围 | 扩展[随机 SVD](/linear-algebra/randomized-svd)的报告证书与单元测试；重放时先核对近似矩阵的行数和每行列数。 |
| 验证 | `python -m unittest projects.linear_algebra_lab.test_randomized_svd`、`npm run content:check`；发布前运行全项目测试、生产构建与 GitHub Pages 工作流。 |
| 差异 | 原实现以嵌套 `zip` 比较数值，截断的最后一行或一列可能不参与比较；现在缺行、缺列和多余维度都被拒绝。 |
| 下一决策 | 继续审计其他报告验证器是否在序列、矩阵或事件轨迹上错误依赖前缀比较；不把单个随机 SVD 小样例外推为大规模误差界。 |

## 正在推进：v3.1「相关观测的真实抽样单位」

### v3.1-a：用户/设备簇级 Bootstrap（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能区分逐条、时间块与用户/设备簇三种重采样单位，并知道同一簇多条记录不等于同样多的独立证据。 |
| 范围 | 新增[簇级 Bootstrap](/probability-ml/cluster-bootstrap-calibration)课程、`cluster-window-calibration-bootstrap/v1` 报告/证书与反例测试；更新概率导航、成熟度看板、课程规模与优先深度门禁。 |
| 验证 | `python -m unittest projects.naive_bayes_spam.test_cluster_window_calibration_bootstrap`、`npm run content:check`；发布前运行全项目测试、生产构建与 GitHub Pages 工作流。 |
| 差异 | 原有观测级和时间块合同无法表达用户或设备的重复观测；现在每个窗口要求至少两个预定义、唯一标识的完整簇，报告重放簇大小、政策、种子与 ECE 差区间，并拒绝篡改。 |
| 下一决策 | 不把簇级重采样外推为同时存在时间趋势、配对窗口、抽样权重或因果处理的通用方法；先审计这些前提是否真实满足。 |

### v3.1-b：全站公式定界符与渲染器一致（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者打开课程时应看到已渲染的数学，而不是因不受支持的定界符看到原始 TeX。 |
| 范围 | 将 7 篇课程中的 `\\(...\\)` / `\\[...\\]` 统一为当前插件支持的 `$...$` / `$$...$$`；KaTeX 门禁拒绝旧定界符。 |
| 验证 | `npm run content:check`：全站结构检查、旧定界符检查与 KaTeX 严格解析；发布前运行全项目测试、生产构建与 GitHub Pages 工作流。 |
| 差异 | 原构建未把 Markdown 定界符与配置的 `markdown-it-katex` 行为绑定，正确 TeX 仍可能作为文本发布；现在仅允许公开站点实际支持的定界符。 |
| 下一决策 | 保持 `$`/`$$` 作为单一作者约定；若未来更换渲染器，先更新配置与门禁，再有意识地迁移内容，不能在两套语法间静默混用。 |

### v3.1-c：构建产物数学审计（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 确认公式不仅在 Markdown 源码中可解析，也实际出现在 VitePress 输出页面中，且没有 KaTeX 错误节点。 |
| 范围 | 新增 `scripts/check-rendered-math.mjs`，在 VitePress 构建与站点打包之间扫描全部 HTML；跳过代码示例、脚本和样式。 |
| 验证 | `npm run build`：全量内容检查、387 项测试、VitePress 构建与产物扫描；当前构建中 110 个页面包含 KaTeX，未发现错误节点或正文旧定界符。 |
| 差异 | 原门禁只分析 Markdown 文本，无法证明插件最终输出；现在发布会在出现 `katex-error` 或正文原始 `\\(` / `\\[` 时停止。 |
| 下一决策 | 产物扫描不测量视觉溢出或浏览器差异；当具备受控浏览器运行时，再补核心公式页的桌面与移动视觉回归。 |

## 正在推进：v3.2「从统计量到可审计的随机实验」

### v3.2-a：置换检验报告独立重放（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能验证一份 Monte-Carlo 置换检验的统计量、极端次数与 p 值确实来自声明的两组数据、轮数与种子，而非只信任一个打印出的 p 值。 |
| 范围 | 扩展[假设检验与 p 值](/probability-ml/hypothesis-testing)、`PermutationTestResult` 与单元测试；新增 `permutation_test_certificate`，并把该课加入优先深度文章门禁。 |
| 验证 | `python -m unittest projects.naive_bayes_spam.test_permutation_test`、`npm run content:check`；发布前运行全项目测试、生产构建与 GitHub Pages 工作流。 |
| 差异 | 原结果仅保存统计量、p 值和轮数，外部读者无法从报告重放随机过程；现在种子成为报告字段，篡改极端次数或种子均会使证书失败。 |
| 下一决策 | 该证书不验证可交换性、独立性或预注册；继续以这些统计前提是否在正文和合同中可见为标准，而不是为每个数字添加重复证书。 |

### v3.2-b：密码记录 fail-closed 输入合同（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能区分“候选密码不匹配”与“保存的密码记录已损坏”，并知道这两者不应被同一个 `False` 静默吞掉。 |
| 范围 | 扩展[哈希、MAC 与密码存储](/number-theory-crypto/hashing-passwords)、`password_storage.py` 与单元测试；将该课加入优先深度文章门禁。 |
| 验证 | `python -m unittest projects.crypto_toybox.test_password_storage`、`npm run content:check`；发布前运行全项目测试、生产构建与 GitHub Pages 工作流。 |
| 差异 | 原验证函数只检查算法、轮数和盐；现在在派生与安全比较前也要求派生值为恰好 32 字节的 `bytes`，错长度或错类型记录明确报错。 |
| 下一决策 | 此合同只保护教学记录的解释性，不是生产认证格式或事故响应方案；后续审计优先看核心课程是否仍把前提、诊断与业务行动混为一个输出。 |

## 正在推进：v3.3「根轮换的授权与监督边界」

### v3.3-a：阈值信任根轮换审计（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能区分候选发布的验签与根集合自身的治理，并从旧根阈值、纪元和见证证据审计一次轮换提议。 |
| 范围 | 新增[信任根轮换与阈值监督](/number-theory-crypto/trust-root-rotation)、`trust-root-rotation-audit/v1` 教学报告/证书及反例测试；更新密码学导航、项目说明、成熟度计数与优先深度文章门禁。 |
| 验证 | `python -m unittest projects.crypto_toybox.test_root_rotation`、`npm run content:check`；发布前运行全项目测试、生产构建与 GitHub Pages 工作流。 |
| 差异 | 原路线分别解释身份、防回滚与日志，但没有可执行地连接“旧根如何限制新根”；现在报告会拒绝单根或集合外授权、旧纪元和篡改结论，监督证据缺失只能进入人工复核。 |
| 下一决策 | 该模块不验证签名、生成密钥或应用信任库变更；后续先跨专题审计是否存在类似把政策结论、观测证据与自动行动混在一起的缺口。 |

### v3.3-b：重要性采样的对数权重诊断（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能在不让指数溢出/下溢的前提下重放归一化权重诊断，同时不把数值稳定误作提议分布有效性的证明。 |
| 范围 | 扩展[重要性采样诊断](/numerical-computing/importance-sampling-diagnostics)、`importance_sampling.py` 与测试；新增 `log-weight-diagnostics/v1` 并将该课加入优先深度文章门禁。 |
| 验证 | `python -m unittest projects.floating_point_museum.test_importance_sampling`、`npm run content:check`；发布前运行全项目测试、生产构建与 GitHub Pages 工作流。 |
| 差异 | 原有实验只重放有界 $q(x)=2x$ 基准；现在 max-shift 后的对数权重、log-sum-exp、ESS 与最大权重可独立重放，且超大共同偏移不会改变归一化结果。 |
| 下一决策 | 这个小模块不估计积分、不处理 $-\infty$ 权重，也不实现自适应重要性采样；后续审计应优先寻找真实的概念断点，而非为所有权重报告叠加证书。 |

### v3.3-c：顺序批量最短路更新审计（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能区分端点对与稳定边身份，并知道批量图更新必须保留顺序化快照，而不是只比较开始和结束图。 |
| 范围 | 新增[批量最短路更新](/discrete-math/ordered-shortest-path-updates)、`ordered-shortest-path-updates/v1` 实验/证书与反例测试；更新离散数学导航、成熟度计数与优先深度文章门禁。 |
| 验证 | `python -m unittest projects.algorithm_lab.test_batch_shortest_path_updates`、`npm run content:check`；发布前运行全项目测试、生产构建与 GitHub Pages 工作流。 |
| 差异 | 原有单边课程只能以端点/权重差分描述变化；现在每个更新以唯一 ID 定位，每步完整重放 Dijkstra，并拒绝不存在或重复 ID、被改写快照和报告篡改。 |
| 下一决策 | 完整重算是教学 oracle，不是动态算法性能结论；下一轮优先审计其他主线，而不把替换路径数据结构草率塞入小图实验。 |

## 正在推进：v3.4「基础表示到算法选择的桥接」

### v3.4-a：布尔矩阵与稀疏关系复合（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能从存在量词推导布尔矩阵乘法，并按稀疏性和查询模式选择矩阵或邻接表。 |
| 范围 | 新增[布尔矩阵乘法与稀疏图查询](/foundations/boolean-matrix-sparse-queries)，扩展关系实验和测试；更新预备知识导航、课程规模与优先深度门禁。 |
| 验证 | `python -m unittest projects.foundations_lab.test_relations`、`npm run content:check`；发布前运行全项目测试、生产构建与 GitHub Pages。 |
| 差异 | 原关系闭包课说明逻辑组合但未让读者比较具体实现；现在报告重放布尔乘积、稀疏两跳扫描与操作计数，并拒绝篡改。 |
| 下一决策 | 操作计数不等于真实基准；后续只在出现明确学习链断点时继续扩展位并行或更新策略。 |

## 正在推进：v3.5「核心课程的可审计反例」

### v3.5-a：相关证据的联合似然比较（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能算出把相关特征当作独立证据会怎样改变后验，并将“相关性是边界”从一句提醒变成两份可比较的概率模型。 |
| 范围 | 扩展[条件概率与贝叶斯更新](/probability-ml/bayes)、`bayes_update.py` 与单元测试；新增 `correlated-evidence-comparison/v1` 报告/证书，并更新概率项目说明和成熟度看板。 |
| 验证 | `python -m unittest projects.naive_bayes_spam.test_bayes_update`、`npm run content:check`；发布前运行全项目测试、生产构建与 GitHub Pages 工作流。 |
| 差异 | 原课程说明独立性被破坏会重复计数，却没有量化路径；现在同一对边缘似然可并列重放独立连乘和显式联合似然。完全重复的探测器把后验从错误的 $0.64$ 校正为约 $0.308$，篡改联合模型或输出均被拒绝。 |
| 下一决策 | 该证书不从有限轨迹推断真实特征相关性，也不选择更可信的生成模型；下一轮应检查另一条核心路线中是否同样存在“重要前提只以文字出现、却无法在实验中改变结论”的断点。 |

### v3.5-b：子群体审计的声明组宇宙（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能区分“本窗口观测到的组”与“设计阶段声明的审计组”，并看见零样本组属于证据不足而非不在报告中。 |
| 范围 | 扩展[子群体性能](/probability-ml/subgroup-performance-uncertainty)、`subgroup_monitoring.py`、交叉主线测试与成熟度看板；`declared_groups` 进入报告政策和重放证书。 |
| 验证 | `python -m unittest projects.naive_bayes_spam.test_subgroup_monitoring projects.naive_bayes_spam.test_monitoring_learning_chain`、`npm run content:check`；发布前运行全项目测试、生产构建与 GitHub Pages 工作流。 |
| 差异 | 原报告按当前观察到的组生成行，因此无标签的已声明组会静默消失；现在报告按冻结的声明顺序覆盖所有组，零样本组显式得到 `count=0` 与拒绝结论，未声明的观测组或被改写的组宇宙均无法验证。 |
| 下一决策 | 固定组宇宙不能解决多重比较、标签偏差或因果归因；下一轮应只在另一条课程主线存在同类“设计前提未进入输入合同”的证据时继续扩展。 |

### v3.5-c：发布身份的冻结密钥登记表（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能区分“候选包自称可信”与“预先冻结的 issuer/key 身份绑定”，并看见算法、用途和状态为何必须从可信登记表而非候选元数据读取。 |
| 范围 | 扩展[已签名更新为何仍可能回滚](/number-theory-crypto/signed-release-anti-rollback)、[公钥身份与密钥生命周期](/number-theory-crypto/public-key-lifecycle)、`release_policy.py`、测试与玩具箱说明；将策略合同升级为 `signed-release-policy-audit/v2`，加入 `trusted-key-registry/v1`。 |
| 验证 | `python -m unittest projects.crypto_toybox.test_release_policy`、`npm run content:check`；发布前运行全项目测试、生产构建与 GitHub Pages 工作流。 |
| 差异 | 原候选记录可携带 `key_identity_trusted` 和 `key_status` 声明；现在发布策略以冻结登记表按 `(issuer,key_id)` 查找绑定，拒绝登记表外身份、算法或用途不匹配、以及非 active 状态，并让证书同时重放登记表。 |
| 下一决策 | 虚构登记表不是证书链、透明日志、真实撤销服务或验签器；后续应回到其他专题寻找同类“输入自己声称关键事实”的断点，而不是把教学元数据误作安全基础设施。 |

## 正在推进：v3.6「核心链的系统深度化」

### v3.6-a：四个基本子空间的完整报告（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能从同一矩阵实际得到列、行、零与左零空间的基，并用维数和点积检查它们不只是四个名称。 |
| 范围 | 扩展[矩阵的四个基本子空间](/linear-algebra/four-fundamental-subspaces)、`basis.py` 与测试；新增 `fundamental-subspaces/v1` RREF 报告/证书，并将该课加入优先深度门禁与成熟度说明。 |
| 验证 | `python -m unittest projects.linear_algebra_lab.test_basis`、`npm run content:check`；发布前运行全项目测试、生产构建与 GitHub Pages 工作流。 |
| 差异 | 原课程代码只能从已有阶梯形数秩，不能连通四个空间；现在报告从 $A$ 与 $A^T$ 的 RREF 构造基、验证两条秩—零度式和两组正交补，并拒绝被篡改的主元、维数或基。 |
| 下一决策 | RREF 报告仍是容差依赖的小规模教学诊断，不能替代生产 QR/SVD 的有效秩决策；下一轮继续按“尚未有完整实验闭环的核心课”选择，而不是重复已有证书。 |

### v3.6-b：递归树总工作量的完整报告（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能从同一输入重放归并树的层数、分割、每层线性工作与总工作，不只接受最后的 $n\log n$ 结论。 |
| 范围 | 扩展[递推关系与分治复杂度](/discrete-math/recurrences)、`recurrence_trace.py` 与测试；新增 `merge-sort-recursion-tree/v1` 报告/证书，并将该课加入优先深度门禁和算法实验室成熟度说明。 |
| 验证 | `python -m unittest projects.algorithm_lab.test_recurrence_trace`、`npm run content:check`；发布前运行全项目测试、生产构建与 GitHub Pages 工作流。 |
| 差异 | 原课程只返回层级列表；现在报告绑定每层的子问题数、规模与合并量，证书重放连续深度、每层分割 $n$ 个元素和 $n\log_2 n$ 总量，任何层或总量的篡改均被拒绝。 |
| 下一决策 | 这是精确的二的幂工作模型，不是实际耗时或非规则递归的通用分析器；下一轮回到其他专题，寻找会实质改变结论、且尚无完整实验闭环的核心课。 |

### v3.6-c：时间分层簇级 Bootstrap（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能在时间趋势与用户/设备相关同时存在时，区分逐条、按时间块、按簇与“时间层内整簇”四种重采样单位。 |
| 范围 | 新增[时间分层簇级 Bootstrap](/probability-ml/time-stratified-cluster-bootstrap)、`stratified_cluster_calibration_bootstrap.py` 与测试；新合同 `time-stratified-cluster-calibration-bootstrap/v1` 绑定冻结时间层、层内簇、种子、区间和无自动行动政策。 |
| 验证 | `python -m unittest projects.naive_bayes_spam.test_stratified_cluster_calibration_bootstrap`、`npm run content:check`；发布前运行全项目测试、生产构建与 GitHub Pages 工作流。 |
| 差异 | 原有时间块合同会拆开用户簇，簇级合同会混合时间位置；现在每一时间层内只重抽完整簇，并拒绝跨层重复簇、层形状或区间被篡改。 |
| 下一决策 | 此合同不处理跨层重复用户、长程依赖、层边界选择或因果归因；下一轮应在其他专题寻找同等级的真实断点，避免把教学 bootstrap 扩成伪生产统计系统。 |

## v3.7：可定位的有限关系诊断

### v3.7-a：关系性质的反例见证（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能从“关系不传递/不对称”等布尔结论直接定位违反定义的元素对或三元组。 |
| 范围 | 扩展[集合、关系、等价类与偏序](/discrete-math/sets-relations-orders)、`relations.py` 与测试；新增 `finite-relation-diagnostic/v1`，为每个失败性质保存反例并以证书重放。 |
| 验证 | `python -m unittest projects.algorithm_lab.test_relations`、`npm run content:check`；发布前运行全项目测试、生产构建与 GitHub Pages 工作流。 |
| 差异 | 原报告只能返回 `transitive=False`；现在“相差不超过 1”会得到 $(0,1,2)$，明确显示前两条关系存在而 $(0,2)$ 缺失，篡改见证也无法通过重放。 |
| 下一决策 | 规范遍历顺序不等于最小反例的数学证明，有限枚举也不能验证无限域关系；下一轮应审计其他专题的核心课，而不是将本诊断扩大成符号定理证明系统。 |

### v3.7-b：端点奇性的截断积分报告（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能区分函数端点非有限、可积奇性与真正发散，而不把求积器的输入错误混同于积分结论。 |
| 范围 | 扩展[数值积分](/numerical-computing/numerical-integration)、`integration.py` 与测试；新增 `endpoint-power-improper-integral/v1`，对 $\int_0^1x^{-p}\,dx$ 记录截断积分、收敛分类、极限和尾项。 |
| 验证 | `python -m unittest projects.floating_point_museum.test_integration`、`npm run content:check`；发布前运行全项目测试、生产构建与 GitHub Pages 工作流。 |
| 差异 | 原课程仅列出奇点是固定网格的边界；现在 $p=.5$ 可重放极限 2 与遗漏尾项，而 $p=1$、$p=1.5$ 显式发散，改写极限或尾项会被证书拒绝。 |
| 下一决策 | 幂函数的解析分类不适用于一般黑盒奇性、无界域或未知尾部；下一轮应在另一专题寻找真实解释断点，而不以有限采样替代不当积分理论。 |

## v3.8：归约构造的可运行双向性

### v3.8-a：独立集到 CNF-SAT（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能从一个明确的变量/子句构造理解归约方向，并实际检查原独立集判定与 CNF-SAT 判定同为 yes 或 no。 |
| 范围 | 扩展[P、NP 与归约](/discrete-math/p-np-reductions)、`sat_verifier.py` 与测试；新增 `independent-set-to-cnf-sat/v1`，记录图、$k$、CNF、SAT 解码与小图 oracle。 |
| 验证 | `python -m unittest projects.algorithm_lab.test_sat_verifier`、`npm run content:check`；发布前运行全项目测试、生产构建与 GitHub Pages 工作流。 |
| 差异 | 原实验只能比较 SAT 验证与枚举；现在独立集的槽位、排他和边约束构成 CNF，路径图与三角形都可检查归约两端的 yes/no 等价，构造或解码篡改会失败。 |
| 下一决策 | 这是一份 CNF-SAT 教学构造，不是 3-SAT gadget、Cook–Levin 证明或高性能 SAT 求解器；下一轮应回到另一主线寻找解释断点。 |

### v3.8-b：裸 RSA 签名的可乘反例（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能运行并验证“两个有效裸签名相乘仍验证为相关代表元”的反例，理解验签等式为何不等于不可伪造。 |
| 范围 | 扩展[数字签名](/number-theory-crypto/digital-signatures)、`main.py` 与签名测试；新增 `raw-rsa-signature-multiplicativity/v1` 小参数报告/重放，并将该课加入优先深度门禁和玩具箱说明。 |
| 验证 | `python -m unittest projects.crypto_toybox.test_signatures`、`npm run content:check`；发布前运行全项目测试、生产构建与 GitHub Pages 工作流。 |
| 差异 | 原课程文字说明 $s_1s_2$ 的风险；现在代表元 $2,3$ 的两个玩具签名可重放为代表元 $6$ 的有效签名，改写组合代表元或签名会被拒绝。 |
| 下一决策 | 该反例不提供真实填充、签名或攻击代码；标准 Ed25519/RSA-PSS、密钥管理和协议绑定必须继续由经审计库与规范承担。 |

## v3.8 Meta Review：下一阶段的验收方法（已完成）

| 字段 | 记录 |
| --- | --- |
| 观察 | 当前站点有 110 篇课程正文、29 篇优先深度文章门禁、全项目测试与源/渲染双层数学检查；v3.6–v3.8 已交付 7 个彼此不同、公开发布的实质增量。 |
| 有效部分 | “前提 + 反例 + 可重放实验”有效修复了若干会改变结论的断点，例如相关证据、端点奇性和裸签名可乘性。 |
| 风险 | 最近多个版本的实现形式集中在报告/证书；继续沿用会提高维护量，却不必然改善读者独立推导、调试和方法选择能力。 |
| 决策 | v4 不将新增报告作为默认方案。每个版本须先证明补齐错误路径、跨课迁移、决策权衡或读者行动中的至少一项；否则保持现状并继续审计。 |
| 下一审计 | 分别检查线性代数、离散数学、概率、数值与密码学主线中“完成前置后仍无法做下一步选择”的具体学习断点。 |

## 读者如何使用

- 先沿专题首页完成当前主线；
- 想验证某个结论时，运行课程链接的实验与测试；
- 想参与建设时，从 v1.1 的范围或[成熟度看板](/course-maturity)的“下一关键缺口”选择一个题目，并遵循[编辑与发布流程](/editorial-workflow)。

## v4：从“可重放”到“可做度量决策”

### v4.0-a：余弦相似度与欧氏距离的同集对照（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能用同一查询与候选集看见“方向最像”与“绝对量最近”给出不同第一名，并把最终度量选择绑定到任务语义。 |
| 范围 | 扩展[向量与点积](/linear-algebra/vectors-dot-product)、线性代数实验室说明与成熟度看板；新增 `similarity_metrics.py` 和测试，不新增排序证书。 |
| 验证 | `python -m unittest projects.linear_algebra_lab.test_similarity_metrics`、`npm run content:check`；发布前运行全项目测试、生产构建与 GitHub Pages 工作流。 |
| 差异 | 原课程只以文字说明尺度不变性和预算边界；现在 `[1,0]` 对 `[100,0]` 与 `[1,.1]` 在余弦和欧氏距离下分别选择不同首名，读者可先预测、再改输入验证。 |
| 下一决策 | 该实验不解决特征标准化、马氏距离、阈值校准或高维检索评估；下一审计应转向另一专题中同样缺少“前置完成后仍无法做选择”的真实断点。 |

### v4.1-a：已校准概率的成本敏感阈值（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能用同一个已校准概率比较两种错误成本，并知道 $0.5$ 只是对称成本的特殊政策，而不是模型自然给出的行动线。 |
| 范围 | 扩展[生成模型、朴素贝叶斯与逻辑回归](/probability-ml/generative-discriminative-logistic)、概率分类项目说明与成熟度看板；新增 `cost_sensitive_decisions.py` 和测试，不新增决策证书。 |
| 验证 | `python -m unittest projects.naive_bayes_spam.test_cost_sensitive_decisions`、`npm run content:check`；发布前运行全项目测试、生产构建与 GitHub Pages 工作流。 |
| 差异 | 原课程将错误成本留作文字边界；现在 $p=0.2$ 在 $(c_{FP},c_{FN})=(1,1)$ 下建议判负、在 $(1,9)$ 下建议判正，成本相等的阈值点明确返回未决。 |
| 下一决策 | 这不是校准器、成本估计器或自动化高风险决策器；下一审计应检查数值、离散或密码学主线是否也存在“同一输入下可行方法或假设无法比较”的学习断点。 |

### v4.2-a：数值微分的模板定义域选择（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能区分“中心差分在内部通常截断误差更低”与“中心模板必须在两侧都可取样”，并在同一步长下观察这一选择。 |
| 范围 | 扩展[数值微分与步长选择](/numerical-computing/numerical-differentiation)、浮点数错误博物馆说明与成熟度看板；扩展 `differentiation.py` 和测试，不新增差分证书。 |
| 验证 | `python -m unittest projects.floating_point_museum.test_differentiation`、`npm run content:check`；发布前运行全项目测试、生产构建与 GitHub Pages 工作流。 |
| 差异 | 原实验只扫描中心差分；现在 $e^x$ 在内部可并列显示中心误差更小，而 $\log x$ 的左侧点越界时中心结果为 `None`、前向结果仍可见但不被误标为准确。 |
| 下一决策 | 该对照不提供单侧高阶公式、噪声估计、复步长或自动微分实现；下一审计应转向离散或密码学主线寻找同等真实的选择断点。 |

### v4.3-a：DH 密钥确认与身份认证的分界（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能从相同的小参数转录区分“对端确认同一会话元素”与“对端是预期身份”，并看见中间人为何能满足前者而不能建立后者。 |
| 范围 | 扩展[Diffie–Hellman 密钥交换](/number-theory-crypto/diffie-hellman)、密码学玩具箱说明与成熟度看板；扩展 `diffie_hellman.py` 和测试，不实现 MAC、KDF、签名或协议。 |
| 验证 | `python -m unittest projects.crypto_toybox.test_diffie_hellman`、`npm run content:check`；发布前运行全项目测试、生产构建与 GitHub Pages 工作流。 |
| 差异 | 原课程只有诚实/中间人会话值；现在同一中间人转录还显示 Alice—Mallory 与 Bob—Mallory 的确认同时为真，而 Alice—Bob 不共享一条会话。 |
| 下一决策 | 这不是密钥确认、身份认证或密钥协商协议；下一轮应回到离散数学或进行 v4 Meta Review，审查当前“同输入对照”方法是否仍在产生真实学习增量。 |

## v4 Meta Review：从固定对照到读者构造（已完成）

| 字段 | 记录 |
| --- | --- |
| 观察 | v4.0–v4.3 分别覆盖度量、成本、数值模板与协议语义：四次都让同一输入在不同前提下产生不同结论，并要求读者预测、运行与解释。当前站点有 110 篇课程正文、105 篇实际课程、29 篇优先深度文章门禁和 420 项项目测试。 |
| 有效部分 | 不再默认添加证书，而是将隐藏前提变成可改动的输入：尺度是否有意义、错误成本由谁声明、模板点是否可取样、会话一致性是否已绑定身份。 |
| 风险 | 四个增量的实现形式已趋同为“固定案例 + 双路径帮助函数”。继续把每个专题都做成同类比较表，会让读者能解释现成答案，却未必能自己构造模型、反例或验收条件。 |
| 决策 | v5 不以专题覆盖对称性立项。新候选必须优先证明读者可自行构造输入/不变量、跨课传递产物、定位失败轨迹的首个前提破坏，或定义比较方案的外部评价标准；无此证据则只记录审计，不新增版本。 |
| 下一审计 | 先检查离散数学的“贪心—DP”与线性代数的“投影—最小二乘”链中，读者是否能从自然语言任务自行选择目标、状态或验收不变量，而不只运行给定示例。 |

## v5：从给定对照到读者构造

### v5.0-a：投影—最小二乘的建模诊断（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能从任务自行构造 $A,b$，再区别精确可达、最小二乘投影、秩亏和欠定模型，并为每种情况选择不同的验收不变量。 |
| 范围 | 扩展[最小二乘](/linear-algebra/least-squares)、线性代数实验室说明与成熟度看板；新增 `diagnose_least_squares_case` 和测试，不新增报告/证书。 |
| 验证 | `python -m unittest projects.linear_algebra_lab.test_main`、`npm run content:check`；发布前运行全项目测试、生产构建与 GitHub Pages 工作流。 |
| 差异 | 原课程只比较给定良态矩阵的两条求解路径；现在读者输入会被分类为精确 $Ax=b$、不可达目标的 $A^Tr\approx0$、秩亏需解选择或欠定需先声明规则，不能再以小残差冒充唯一答案。 |
| 下一决策 | 此工具不从自然语言提取特征，也不实现秩揭示 QR、SVD 或鲁棒回归；下一审计应检查离散“贪心—DP”链能否同样要求读者定义状态与验证不变量。 |

### v5.1-a：贪心—DP 的任务合同与目标诊断（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能先把排程题意写成区间语义、优化目标、活动字段和验收不变量，再让这个合同选择贪心或 DP，而不从给定样例反推方法。 |
| 范围 | 扩展[贪心算法](/discrete-math/greedy-exchange-arguments)、[动态规划](/discrete-math/dynamic-programming-dag)、算法实验室说明与成熟度看板；在 `weighted_activity.py` 新增 `diagnose_activity_task` 和测试，不新增执行证书。 |
| 验证 | `python -m unittest projects.algorithm_lab.test_weighted_activity`、`npm run content:check`；发布前运行全项目测试、生产构建与 GitHub Pages 工作流。 |
| 差异 | 原课程让读者对固定无权/加权表执行两条路径；现在同一时间窗在 `maximize_count` 下得到最早结束与最大基数不变量，在 `maximize_value` 下得到前缀 DAG、最大总价值不变量和最早结束的实际差距。目标与验收量错配、闭区间语义或不匹配字段会在选择算法前被拒绝。 |
| 下一决策 | 这不是自然语言提取、真实排班器、多资源调度或大规模 oracle；下一轮应审计其他主线是否也仍把“任务目标/验收条件”留在文字中，而不是机械复制活动合同。 |

### v5.2-a：求根的保证等级合同（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能在调用求根器前声明需保留的数学保证、导数可用性、容差和预算，并据此区分受保护 Newton 与只给局部残差证据的割线。 |
| 范围 | 扩展[牛顿法](/numerical-computing/newton-method)、[割线法](/numerical-computing/secant-method)、浮点数错误博物馆、成熟度看板与路线图；在 `root_finding.py` 新增 `diagnose_root_task` 和测试，不新增轨迹证书。 |
| 验证 | `python -m unittest projects.floating_point_museum.test_root_finding`、`npm run content:check`；发布前运行全项目测试、生产构建与 GitHub Pages 工作流。 |
| 差异 | 原实验的区间、导数、容差和停止量由示例调用隐含；现在“残差加保留变号区间”合同必须提供导数与区间内初值，得到受保护 Newton；“只要局部残差”合同不得夹带未使用初值，得到割线并明确没有全局区间结论。 |
| 下一决策 | 此工具不能证明连续性、唯一根或物理模型正确，也不实现 Brent、多维根或真实停止政策；下一轮应审计另一主线是否存在同样的“保证等级只写在文字中”的断点。 |

## v5.2 Meta Review：从“写合同”到“跨课使用产物”（已完成）

| 字段 | 记录 |
| --- | --- |
| 观察 | v5.0–v5.2 已让读者分别构造线性模型、排程目标和求根保证。当前站点有 110 篇课程正文、105 篇实际课程、29 篇优先深度文章门禁和 428 项项目测试。 |
| 审计 | 概率线的置信区间、带标签窗口和观测/时间块/簇级 bootstrap 已有抽样单位、区间、固定窗口与 `automatic_action: none` 的可运行合同；新增一层相同的合同选择器不会形成新的学习动作。 |
| 风险 | 若继续把每次升级都做成“读者填字段 → 函数选方法”，会把 v5 的构造能力收缩为记忆标签，且再次堆积报告/证书或固定双路径比较。 |
| 决策 | v5.3 起，版本必须让读者把上游课程的实际产物传到下游，并导致可检查的结论、可接受方案或首个失败前提变化；独立的合同、字段、证书或固定对照不再足够。 |
| 下一审计 | 优先检查“抽样单位/置信区间 → 业务最小效应与人工复核”能否形成不自动化高风险行动的真实数据流；找不到明确断点则保持现状。 |

### v5.3-a：均值差区间到最小效应人工复核（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能把上游两组均值差区间原样交给下游最小实际效应复核，区分统计不确定性与业务门槛，而不将任一结果升级为自动行动。 |
| 范围 | 扩展[抽样误差、置信区间与覆盖率](/probability-ml/confidence-intervals-sampling)、概率项目说明、成熟度看板与路线图；新增 `practical_effect_review.py` 与测试。 |
| 验证 | `python -m unittest projects.naive_bayes_spam.test_practical_effect_review`、`npm run content:check`；发布前运行全项目测试、生产构建与 GitHub Pages 工作流。 |
| 差异 | 原课程把区间、MDE 与业务决策并列说明；现在下游仅接受可重新计算的上游区间产物，输出整体高于、低于或穿过最小效应。篡改区间端点、差值或标准误不能流入复核，且三种状态都固定 `automatic_action: none`。 |
| 下一决策 | 正态近似教学产物不能证明随机化、因果、长期价值、群体公平或阈值正当性；下一轮应寻找另一条真实跨课数据流，而不是将这份人工复核包装成 A/B 决策器。 |

### v5.4-a：透明日志追加产物到根轮换复核（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能把透明日志的实际追加产物带入根轮换复核，检查新增根是否出现在未改写日志中，并区分日志覆盖与身份/批准。 |
| 范围 | 扩展[透明日志](/number-theory-crypto/transparency-logs)、[信任根轮换](/number-theory-crypto/trust-root-rotation)、密码学玩具箱、成熟度看板与路线图；在 `root_rotation.py` 新增 `root_rotation_log_link_review` 和测试。 |
| 验证 | `python -m unittest projects.crypto_toybox.test_root_rotation`、`npm run content:check`；发布前运行全项目测试、生产构建与 GitHub Pages 工作流。 |
| 差异 | 原根轮换只看到见证可用布尔值；现在接受可重放追加日志并从根集合导出应出现的条目。新增根条目缺失时下游报告精确列出缺项，日志被改写时不能进入复核；覆盖完整仍只给人工复核。 |
| 下一决策 | 这不是日志一致性协议、签名验证、身份绑定、独立见证或根管理器；下一轮应审计不同专题是否还存在上游产物在课程边界被退化成布尔值的真实断点。 |

### v5.5-a：范围发现产物到随机 SVD（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能把已生成且可重放的随机范围报告直接传给随机 SVD，并在下游确认实际分解的子空间来源。 |
| 范围 | 扩展[随机范围发现](/linear-algebra/randomized-range-finder)、[随机 SVD](/linear-algebra/randomized-svd)、线性代数实验室、成熟度看板与路线图；新增 `randomized_svd_from_range_report` 和来源篡改测试。 |
| 验证 | `python -m unittest projects.linear_algebra_lab.test_randomized_range projects.linear_algebra_lab.test_randomized_svd`、`npm run content:check`；发布前运行全项目测试、生产构建与 GitHub Pages 工作流。 |
| 差异 | 原随机 SVD 内部重新运行范围发现，即使读者已有上游报告也无法证明下游使用了同一 $Q$；现在下游仅接受能对同一矩阵、seed、过采样和幂迭代重放的来源，保留 `source_range_report`，篡改来源或换矩阵都会被拒绝。 |
| 下一决策 | 这不是随机 SVD 的高概率误差界、稳定 QR、稀疏/分布式实现或自动调参器；下一轮应继续审计是否仍有课程把实际上游产物退化成文字、布尔值或隐式重算。 |

### v5.6-a：随机 SVD 重构到 MSE 预算评审（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能将一份已验证的随机 SVD 重构直接交给图像误差评审，得到可追溯的像素指标与声明 MSE 预算的关系。 |
| 范围 | 扩展[随机 SVD](/linear-algebra/randomized-svd)、[图像误差指标](/linear-algebra/image-error-metrics)、线性代数实验室、成熟度看板与路线图；新增 `randomized_svd_image_quality_review` 和测试。 |
| 验证 | `python -m unittest projects.linear_algebra_lab.test_image_metrics projects.linear_algebra_lab.test_randomized_svd`、`npm run content:check`；发布前运行全项目测试、生产构建与 GitHub Pages 工作流。 |
| 差异 | 原误差课接收匿名近似矩阵，无法确认它来自哪一份随机草图；现在评审先重放随机 SVD 来源，再使用实际重构计算 MSE、RMSE、PSNR、最大误差。相同重构在不同 MSE 预算下会明确得到预算内或超出，篡改来源、重构、参考图或结论都会被拒绝。 |
| 下一决策 | 这不是感知指标、文件编码器、图像检索/分类验证或自动发布器；即使数值预算内也固定 `automatic_action="none"`。下一轮应继续寻找其他专题中可被真实上游产物改变的下游结论。 |

### v5.7-a：Platt 校准产物到成本敏感决策（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能将独立验证集拟合的、可重放 Platt 校准产物交给成本敏感决策，而不是把一个匿名概率直接送入阈值公式。 |
| 范围 | 扩展[概率再校准](/probability-ml/recalibration)、[生成模型、朴素贝叶斯与逻辑回归](/probability-ml/generative-discriminative-logistic)、概率分类项目、成熟度看板与路线图；新增 `PlattCalibrationReport`、`calibrated_cost_sensitive_decision` 及测试。 |
| 验证 | `python -m unittest projects.naive_bayes_spam.test_recalibration projects.naive_bayes_spam.test_cost_sensitive_decisions`、`npm run content:check`；发布前运行全项目测试、生产构建与 GitHub Pages 工作流。 |
| 差异 | 原成本函数只接收裸概率；现在下游先重放验证分数、标签、超参数、Platt 参数和优化轨迹，再转换保留分数。同一原始 `.9` 在 $c_{FP}=4,c_{FN}=1$ 下从裸概率的判正变为校准后约 `.75` 的判负；篡改校准产物或决策结论都会被拒绝。 |
| 下一决策 | 这不是测试集上的重新拟合、在线校准、成本估计、因果证明或自动化高风险行动；所有输出固定 `automatic_action="none"`。下一轮继续审计是否还有上游学习产物被下游简化为匿名标量。 |

### v5.8-a：受保护 Newton 轨迹到位置误差预算（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能将一份已验证的受保护 Newton 轨迹带入位置精度评审，分开“小残差”和“由保留区间支持的位置误差上界”。 |
| 范围 | 扩展[牛顿法](/numerical-computing/newton-method)、浮点数错误博物馆、成熟度看板与路线图；新增 `BracketedRootPositionReview`、`bracketed_root_position_review` 与测试。 |
| 验证 | `python -m unittest projects.floating_point_museum.test_root_finding`、`npm run content:check`；发布前运行全项目测试、生产构建与 GitHub Pages 工作流。 |
| 差异 | 原课程只让读者观察残差与符号区间；现在下游先重放该轨迹，在明确连续性为外部模型前提后，以返回估计到末区间两端的最大距离评审位置预算。$x^2-2$ 的 Newton 运行虽有极小残差，末区间仍可超出 $10^{-6}$；篡改轨迹、端点、上界或结论都会被拒绝。 |
| 下一决策 | 这不是连续性/唯一根证明、导数下界误差估计、条件数分析、Brent 实现、多维求根或自动控制授权；所有结果固定 `automatic_action="none"`。继续审计其他专题中的“数值结果被误当作足够误差证据”断点。 |

### v5.9-a：PCA 的谱推导与 SVD 统一（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能从协方差的半正定性、Rayleigh 商和约束优化推导主子空间，而不是把“最大特征值”当作无来源的规则。 |
| 范围 | 深化[特征值与 PCA](/linear-algebra/eigenvalues-pca)、线性代数导航、成熟度看板与路线图；补充二维谱例、k 维子空间目标和中心化 SVD 等价，不新增教学算法。 |
| 验证 | `npm run content:check`；发布前运行全项目测试、生产构建、渲染数学检查与 GitHub Pages 工作流。 |
| 差异 | 原正文给出 $w^TCw$ 后直接陈述最大特征值方向；现在先证明协方差对称半正定，再由拉格朗日条件得到特征方程、由 Rayleigh 商给出最大性，并把解释方差、残差能量与 SVD 的右奇异向量置于同一推导链。 |
| 下一决策 | 这不证明真实样本的统计代表性、因果价值、业务价值或鲁棒性，也不实现高维 PCA/SVD 库；下一审计应检查其他核心证明是否仍从定义跳到结论而遗漏可复用的中间不变量。 |

### v5.10-a：最大流最小割的残量重排与对偶证明（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能手算反向残量如何撤销一次历史流，并从流守恒完整推出“流值 = 最小割容量”，而不是把定理当作黑箱。 |
| 范围 | 深化[最大流最小割](/discrete-math/max-flow-min-cut)、离散数学导航、成熟度看板与路线图；修正反向边示例并区分抽象邻接表界和本站稠密矩阵实现成本，不新增教学算法。 |
| 验证 | `npm run content:check`；发布前运行全项目测试、生产构建、渲染数学检查与 GitHub Pages 工作流。 |
| 差异 | 原正文只断言反向边可修正选择、任意流不超过任意割；现在用 $s\to b\to a\to t$ 明示撤销 $a\to b$ 的流，以割内散度求和得到弱对偶，并由终止残量可达集的饱和/零流性质得到强对偶等式。 |
| 下一决策 | 这不实现 Dinic、最小费用流、多商品流或生产级浮点流求解器；下一审计继续寻找“从定义跳到定理、或抽象复杂度掩盖实现成本”的核心课。 |

### v5.11-a：最大似然的经验交叉熵与 KL 语义（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 读者能从伯努利观测推出 MLE 与训练交叉熵是同一目标，并准确分开 MLE、MAP 与后验预测。 |
| 范围 | 深化[最大似然](/probability-ml/maximum-likelihood)、概率论导航、成熟度看板与路线图；补充经验交叉熵/KL 分解、有限样本数值表和同例 MLE/MAP/预测比较，不新增模型训练接口。 |
| 验证 | `npm run content:check`；发布前运行全项目测试、生产构建、渲染数学检查与 GitHub Pages 工作流。 |
| 差异 | 原正文从导数直接得到样本均值；现在证明 $-\ell/n=H(\hat q,p)=H(\hat q)+D_{\mathrm{KL}}(\hat q\Vert p)$，说明优化的是经验分布内的 KL 投影，并以 Beta$(2,2)$、一次正面的具体值区分三种估计目标。 |
| 下一决策 | 这不证明大样本一致性、选择真实模型、提供置信区间或训练生产分类器；下一审计继续优先处理核心课程中“记住结论但没有目标函数语义”的断点。 |

### v5.12-a：本地搜索索引的课程优先预算（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 保留无需外部服务的全文课程搜索，同时避免版本记录、编辑流程和模板挤占读者下载的搜索索引。 |
| 范围 | 为辅助页面标记 `search: false`；新增构建期搜索索引预算与内容边界校验，不改变任何课程或项目页面的搜索可见性。 |
| 验证 | `npm run build` 需验证一个本地搜索索引低于 1.5 MiB、课程标题仍可检索、四份辅助页面标题不再进入索引。 |
| 差异 | 原索引约 1,723 KiB，且收录大体量版本流水与内部路线；现在约 1,459 KiB，减少约 15%，并由构建门禁防止辅助内容回流。 |
| 下一决策 | 这不是全文搜索的相关性评估、gzip 传输测量、外部搜索服务迁移或 MPA 架构切换；若课程正文增长使预算失效，应先审计索引内容与加载行为，再决定是否引入专门搜索方案。 |

### v5.13-a：交互实验的选中状态无障碍语义（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 让读屏与键盘用户获知形状、集合运算和浮点案例实验中当前选择的状态，而不是只能从颜色推断。 |
| 范围 | 为三组视觉高亮的原生按钮加入动态 `aria-pressed`；新增构建期组件语义校验，不改变实验的计算、键盘顺序或样式。 |
| 验证 | `npm run build` 检查三个组件均具有与各自状态变量绑定的 `aria-pressed`，并继续执行内容、项目、渲染数学和搜索预算检查。 |
| 差异 | 原按钮组只有 `active` CSS 类，辅助技术不知道哪项已选；现在每个原生按钮公开真/假按下状态，构建会拒绝未来删除这些绑定。 |
| 下一决策 | 这不是完整 WCAG 审计、屏幕阅读器实机测试、键盘焦点顺序重构或图形替代文本的全面重写；下一轮应在真实浏览器中抽样验证交互与小屏布局。 |

### v5.14-a：交互结论的动态播报（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 让辅助技术用户在改变形状操作或集合运算后，能获得新结论本身，而不只知道一个按钮被按下。 |
| 范围 | 为形状结论和集合运算结果加入 `aria-live="polite"`，并扩展同一构建期无障碍校验；不改变公式、结果、控件顺序或视觉样式。 |
| 验证 | `npm run build` 检查两块动态结果均有礼貌播报区，并保留状态按钮、公式、搜索和项目测试门禁。 |
| 差异 | v5.13 后读屏器可获知“矩阵乘法已选中”，但不保证随后读出 $(2,3)@(3,2)\to(2,2)$ 或并/交/差集结果；现在结果区域本身成为更新通知。 |
| 下一决策 | 这不覆盖拖放、异步加载、图像替代、色彩对比或所有屏幕阅读器组合；下一次可把抽样浏览器结果扩展为可重复的小屏和键盘交互检查。 |

### v5.15-a：基坐标、可达性与近似入口（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 让读者从“线性组合”走到可推导的唯一坐标、可手算的精确可达性判据，以及无精确解时正确进入最小二乘，而不是把基、秩和降维只当作术语。 |
| 范围 | 深化[线性组合、基与维度](/linear-algebra/linear-combinations-basis)，并将它加入优先深度文章门禁；同步成熟度与路线图，不新增玩具算法。 |
| 验证 | `npm run content:check` 检查该页的完整深度结构；发布前运行全部项目测试、构建、数学源/渲染、搜索和交互语义门禁，以及 GitHub Pages 工作流。 |
| 差异 | 原文由定义直接跳到“基坐标唯一”和“数据近似时转最小二乘”；现在从 $Ac=Ad$ 推出 $A(c-d)=0$ 并用无关性证明唯一性，以 $\mathrm{rank}(A)=\mathrm{rank}([A\mid b])$ 区分不可达目标和可达但冗余的列，再把前者精确连接为最小二乘的 $A^Tr\approx0$ 入口。 |
| 下一决策 | 这不实现秩揭示 QR、SVD、稀疏回归或自动特征选择；下一轮应从其余核心路线选择仍缺少“定义到可手算判据或算法不变量”桥梁的页面，而非继续为相邻课程增加同类文字。 |

### v5.15-b：命名数学算子的渲染回归修复（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 修复正文中被当作普通文本显示的命名数学算子，让读者看到可读的 KaTeX 公式而不是反斜杠命令。 |
| 范围 | 将 48 篇 Markdown 中的 `\operatorname{...}` 统一为受当前渲染器支持的 `\mathrm{...}`；扩展渲染产物门禁以拒绝正文中的遗留命令。 |
| 验证 | `npm run build` 必须完成源公式、渲染 HTML、搜索和交互门禁；公开页面需在浏览器中呈现新增的列空间、秩与可达性公式。 |
| 差异 | 原数学门禁会拒绝 KaTeX 错误节点和旧定界符，却没有识别“命令被原样送进正文”的静默降级；现在已修复全站已知实例，并让构建在同一症状重现时失败。 |
| 下一决策 | 这不是对所有数学记号语义的自动证明，也不替代小屏版式审阅；下一轮应抽样核对不同专题的复杂公式在真实浏览器中的可读性。 |

### v5.16-a：现代 KaTeX 渲染器与静默回退阻断（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 让所有已被 `$...$` 或 `$$...$$` 正确定界的公式真正交给现代 KaTeX 渲染，杜绝旧插件在不支持命令时直接将原始 TeX 回退到正文。 |
| 范围 | 保留 `markdown-it-katex` 的定界符解析，改用项目顶层 KaTeX 渲染 token；将渲染产物检查扩展到任意读者可见的反斜杠命令。 |
| 验证 | 完整构建必须通过；受影响的模运算、图论、概率、数值和线性代数页面需公开抽样验证公式为 KaTeX 节点且无原始命令。 |
| 差异 | 旧实现使用插件内嵌的早期 KaTeX，并在失败时返回裸文本；新实现失败即构建失败，成功则用项目锁定的 KaTeX 版本输出。生产构建从 116 个含 KaTeX 页面增至 120 个，模运算学习目标与含集合花括号的图表示页均已通过渲染抽样。 |
| 下一决策 | 这不判断每条数学推导的正确性，也不替代移动端版式审阅；下一轮回到内容审计，优先处理读者仍无法由定义走到可手算判据或算法不变量的核心课。 |

### v5.17-a：快速幂的逐位不变量证明（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 让读者能逐行手算并证明平方—乘的状态转换，而不是只接受“二进制分解所以很快”的结论。 |
| 范围 | 深化[模运算与快速幂](/number-theory-crypto/modular-arithmetic)，以现有 `mod_pow_trace` 为依据增加 $3^{13}\bmod7$ 轨迹表、两种最低位的代数保持证明、篡改证书解释与升级练习；加入优先深度文章门禁。 |
| 验证 | `npm run content:check` 检查完整深度叙事与公式；发布前运行全部项目测试、构建、渲染数学、搜索、无障碍门禁与 GitHub Pages 工作流。 |
| 差异 | 原文只陈述 $\mathrm{result}\cdot\mathrm{base}^{\mathrm{exponent}}\equiv a^E$；现在读者能在 1、0、1、1 四个最低位上分别验证状态，并由 $q=2q'+b$ 推导两个分支都保持不变量、右移保证终止。 |
| 下一决策 | 这不实现 Montgomery ladder、常量时间大整数、真实密钥或攻击工具；下一轮应审计其他专题中是否仍有“算法给出不变量但正文没有连接具体轨迹”的同类断点。 |

### v5.18-a：扩展欧几里得的余数—回代桥（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 让读者从欧几里得除法链亲手推出贝祖系数与模逆元，而不是只接受递归代码恰好返回一对系数。 |
| 范围 | 深化[扩展欧几里得与模逆元](/number-theory-crypto/extended-euclid)：增加 $240,46$ 的整除表、完整反向代入、$7^{-1}\bmod26$ 迁移、递归规格证明与篡改商练习；将该课加入优先深度文章门禁。 |
| 验证 | `npm run content:check` 检查完整深度结构和公式；发布前运行全部项目测试、生产构建、数学渲染/搜索/无障碍门禁与 GitHub Pages 工作流。 |
| 差异 | 原文从 $\gcd(a,b)=\gcd(b,a\bmod b)$ 直接跳到一行系数替换；现在余数下降表与回代共享同一组除法，读者可核对 $2=-9\cdot240+47\cdot46$，并由 $r=a-qb$ 推出递归帧为何返回 $(g,y_1,x_1-qy_1)$。 |
| 下一决策 | 这不实现恒定时间算法、负值 API 规范化策略、大整数性能或真实密钥操作；下一轮应跨专题审计尚未形成“具体计算—定义/证明—工程行为”闭环的核心页面。 |

### v5.19-a：Kahan 补偿的逐步舍入轨迹（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 让读者看见一个丢失的低位如何经补偿状态进入下一轮加法，而不是只接受 Kahan 在示例上“碰巧得到 2”。 |
| 范围 | 深化[Kahan 与 pairwise 求和](/numerical-computing/kahan-summation)：为 $[10^{16},1,1,-10^{16}]$ 增加 binary64 状态表、局部补偿推导和调试练习；在浮点项目加入 `KahanEvent`、`kahan_sum_trace` 与 `kahan_sum_trace_certificate`，并将课程加入优先深度门禁。 |
| 验证 | `python -m unittest projects.floating_point_museum.test_examples` 需验证重放、修正项和篡改拒绝；发布前运行内容检查、全项目测试、生产构建、数学渲染/搜索/无障碍门禁与 GitHub Pages 工作流。 |
| 差异 | 原文有算法伪代码却没有可观察中间量；现在第二轮的 $c=-1$、第三轮的 $y=2$ 和 $t=10^{16}+2$ 同时出现在手算表、运行轨迹与证书中，读者能定位错误补偿符号首先破坏的状态。 |
| 下一决策 | 这不证明任意浮点序列的前向误差界，也不替代整数/十进制定点、固定归约树或高精度库；下一轮应优先审计其他专题中仍只有最终输出而缺少可操作中间判据的关键课程。 |

### v5.20-a：预条件 CG 的同问题成本比较（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 让“Jacobi 为什么可能更快”成为同一 SPD 输入上的可检查对照，而不是独立展示一条已预条件轨迹。 |
| 范围 | 为[预条件共轭梯度法](/numerical-computing/preconditioned-conjugate-gradient)加入对角缩放的手算推导、identity/Jacobi 同输入比较与成本边界；教学求解器和重放器接收明确的预条件选择，并将课程加入优先深度门禁。 |
| 验证 | python -m unittest projects.floating_point_museum.test_preconditioned_cg 必须验证 3 步 identity 与 1 步 Jacobi 的同解结果、两条重放和未知选择拒绝；发布前运行全项目测试、内容/数学门禁、生产构建与 GitHub Pages 工作流。 |
| 差异 | 原代码硬编码 Jacobi，原正文只能说预条件器“改善谱分布”；现在 $A=\mathrm{diag}(1,100,10000)$ 上显式展示 $M^{-1}A=I$、$\alpha_0=1$ 和可复现的 3 对 1 步差异，同时避免把该特殊对角例推广为一般性能保证。 |
| 下一决策 | 这不实现稀疏存储、不完全分解、多重网格、真实运行时基准或自动预条件选择；下一轮应检查离散/概率/线性代数主线是否仍有同一输入下缺少方案比较的核心断点。 |

### v5.20-b：路线图的近期/早期 v5 分段（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 让长期读者能按阶段而非文件追加顺序理解 v5 的最新状态，避免把近期 v5.15–v5.20 误读为早期 v5.0–v5.4 之前的规划。 |
| 范围 | 仅重组[课程深度升级路线图](/course-depth-roadmap)中的标题层级：显式标出近期深化迭代与早期 v5 历史段，保留所有既有记录和链接。 |
| 验证 | 内容门禁和生产构建必须通过；公开路线图的标题顺序需同时呈现两个阶段并保持搜索排除。 |
| 差异 | 原路线图将 v5.15–v5.20 插在 v5.0 总标题之前，读者只能依靠版本号自行推断顺序；现在近期段先说明其依赖的早期基础，下方早期段给出 v5.0–v5.4 的原始设计演进。 |
| 下一决策 | 这不重写历史版本、不修改课程内容或制造新的教学能力；下一次实质版本必须回到跨专题的真实学习断点，而非继续调整版本叙事。 |

### v5.21-a：公式感知的课程搜索预算（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 在课程持续增加推导公式时，保留本地全文搜索的课程内容与公式检索能力，同时清除 KaTeX 同一公式的三份索引重复。 |
| 范围 | 为 VitePress 本地搜索增加仅索引侧使用的公式降重渲染：行内/块级公式各保留一次转义 TeX；将搜索预算由 1.5 MiB 收紧至 1.35 MiB，并验证课程标题、正文术语与公式命令词。 |
| 验证 | `npm run build` 必须通过内容、441 项项目测试、KaTeX 渲染、搜索预算和交互无障碍门禁；索引实际为 1363.9 KiB，且不含 KaTeX 的 MathML 类名。 |
| 差异 | 原索引约 1484.7 KiB，公式的 MathML、可视 HTML 和 TeX 注释都被重复分词；现在约 1363.9 KiB，释放约 121 KiB，同时保留“最大流最小割”、 “残量网络”与 `lambda` 的可检索证据。 |
| 下一决策 | 这不衡量搜索排序、传输压缩或公式语义理解；下一次内容升级先回到跨专题 P0/P1 课程审计，只有发现可证明的学习断点才扩大正文或实验。 |

### v5.22-a：全方差的直接混合验算（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 让读者能以同一份有限联合模型独立计算全方差的条件分解与直接混合值，而不是把报告字段的自我相加误认为验证。 |
| 范围 | 深化[期望、方差与不确定性](/probability-ml/expectation-variance)：增加两条手算路径；报告新增直接混合、分解和差额字段；新增重放证书及篡改拒绝测试，并纳入优先深度文章门禁。 |
| 验证 | `python -m unittest projects.naive_bayes_spam.test_moments` 通过 5 项测试；全项目 442 项测试与 `npm run build` 均通过，包含内容/公式、搜索预算和交互无障碍门禁。发布后检查公开课程的 KaTeX 表格和 GitHub Pages 工作流。 |
| 差异 | 旧 `total_variance_report` 只返回 $E[\mathrm{Var}]+\mathrm{Var}(E)$ 的和，无法发现它与由同一联合表直接求得的方差不一致；新报告保留两条路径和差额。 |
| 下一决策 | 待验证和发布后，再审计其余 P0/P1 链中是否存在类似“报告只重述定义、没有独立重算”的证据断点；不为形式添加证书。 |

### v5.23-a：相关日志的有效样本量反例（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 让读者在看到 $1/\sqrt n$ 前能手算并复跑一个“行数增加但独立信息不增加”的反例，避免将日志数误当有效样本量。 |
| 范围 | 深化[大数定律与中心极限定理](/probability-ml/laws-of-large-numbers-clt)：增加成对复制的代数推导和固定种子实验；新增报告重放证书、篡改拒绝测试，并纳入优先深度文章门禁。 |
| 验证 | `python -m unittest projects.naive_bayes_spam.test_sampling_limit_laws` 通过 4 项测试；全项目 443 项测试与 `npm run build` 均通过，包含内容/公式、搜索预算和交互无障碍门禁。发布后检查公开课程的公式与 GitHub Pages 工作流。 |
| 差异 | 旧课只口头说明相关性会增加标准误；新课在边缘概率不变的复制模型中精确得到 $\sqrt2$ 标准误膨胀，并将它与经验重复试验对照。 |
| 下一决策 | 待验证和发布后，再选择其他专题中“口头边界未形成可算反例”的核心课；不把这一复制模型外推为通用簇稳健推断。 |

### v5.24-a：插值节点选择的同网格对照（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 让读者在同一函数、同一节点数和同一评估网格下比较等距与 Chebyshev 节点，而不是把“Runge 现象”作为没有量纲的警告记忆。 |
| 范围 | 深化[数值插值](/numerical-computing/interpolation)：新增固定 Runge 函数的节点比较报告、共享网格最大/边缘误差、重放证书与篡改拒绝测试，并纳入优先深度文章门禁。 |
| 验证 | `python -m unittest projects.floating_point_museum.test_interpolation` 通过 5 项测试；全项目 445 项测试与 `npm run build` 均通过，包含内容/公式、搜索预算和交互无障碍门禁。发布后检查公开课程的 KaTeX 与 GitHub Pages 工作流。 |
| 差异 | 原课程只验证每个多项式在自己的节点精确，并以文字解释 Runge 现象；新报告控制函数、节点数和网格后，直接比较端点振荡幅度与全局最大误差。 |
| 下一决策 | 待验证和发布后，再审计其他专题中“选择建议有文字理由但没有受控同问题比较”的课程；不宣称有限网格结果是通用误差界。 |

### v5.25-a：Gram–Schmidt 的正交性与重构分离（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 让读者区分 QR 的重构误差与 Q 的正交性缺陷，并在同一近共线输入上理解为何经典与改进 Gram–Schmidt 的浮点质量不同。 |
| 范围 | 深化[正交投影、Gram–Schmidt 与 QR 分解](/linear-algebra/orthogonal-projection-qr)：新增 CGS/MGS 同输入报告、正交缺陷/重构误差与重放证书、篡改拒绝测试，并纳入优先深度文章门禁。 |
| 验证 | `python -m unittest projects.linear_algebra_lab.test_main` 通过 30 项测试；全项目 446 项测试与 `npm run build` 均通过，包含内容/公式、搜索预算和交互无障碍门禁。发布后检查公开课程的 KaTeX 与 GitHub Pages 工作流。 |
| 差异 | 旧课文字上区分经典和改进 Gram–Schmidt，却没有数据证明“重构接近 A”不足以保证“Q 仍正交”；新报告在同一近共线三列上同时暴露两个量。 |
| 下一决策 | 待验证和发布后，继续跨专题审计“一个正确性指标遮蔽另一关键不变量”的课程；不将这个三列教学反例夸大为所有 QR 实现的性能基准。 |

### v5.26-a：RSA-CRT 单分支故障的 gcd 证据（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 让读者从两个 CRT 输出的同余关系推出为何单分支故障可令差值的 gcd 暴露另一质因子，而不是把 RSA-CRT 故障边界当作无机制的警告。 |
| 范围 | 深化[中国剩余定理](/number-theory-crypto/chinese-remainder-theorem)：增加小参数单分支残余故障报告、gcd 重放证书与篡改拒绝测试，并纳入优先深度文章门禁。 |
| 验证 | `python -m unittest projects.crypto_toybox.test_chinese_remainder` 通过 5 项测试；全项目 448 项测试与 `npm run build` 均通过，包含内容/公式、搜索预算和交互无障碍门禁。发布后检查公开课程的 KaTeX 与 GitHub Pages 工作流。 |
| 差异 | 原课说明“常可由 gcd 恢复因子”，却没有将一个正确/错误分支、差值同余和实际 gcd 放入同一可检查样例；新报告用显式小整数绑定这些量。 |
| 下一决策 | 待验证和发布后，继续选择有明确数学机制但尚未形成可复跑边界例的课程；不将此受限教学演示扩展为现实攻击或密钥操作能力。 |

## v5.27 Meta Review：从“可重放报告”到“独立不变量”（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 复核近期六个版本是否仍产生不同的读者能力，而不是把重放证书变成每门课都要添加的模板。 |
| 范围 | 审计 v5.21–v5.26 的共同结构、DFS 的现有证据闭环与优先深度门禁覆盖；在[课程深度升级路线图](/course-depth-roadmap)写入下一阶段的共享输入、独立不变量和结论边界门槛。 |
| 验证 | 审计当前 Git 历史、课程正文、`scripts/check-content.mjs` 的 39 篇优先深度课及全项目 448 项测试；发布前运行内容/公式、构建、搜索与无障碍门禁。 |
| 差异 | 旧路线只要求候选课存在真实学习断点；新规则进一步拒绝“单一正确指标”或“同类证书重复”，要求读者能在共享输入下比较两条独立量，或用受限反例定位前提。 |
| 下一决策 | 后续版本不按专题配额推进；仅在发现符合新门槛的断点时实施，否则保留基线、继续审计，不制造版本号。 |

## v5.28-a：幂迭代的初值盲区与残差对照（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 让读者在同一矩阵上区分“小残差证明近似特征对”与“得到主特征对”这两个不同结论。 |
| 范围 | 深化[幂迭代](/linear-algebra/power-iteration)，使 `dominant_eigenpair` 接受已声明初值；新增初值敏感性报告与重放证书、盲区反例测试，并纳入优先深度文章门禁和成熟度看板。 |
| 验证 | `python -m unittest projects.linear_algebra_lab.test_power_iteration` 通过 5 项测试；发布前运行全项目测试、内容/公式、搜索、无障碍与静态构建门禁。 |
| 差异 | 旧实现固定均匀初值，读者只能观察到主方向成功；新实验对同一 $A=\mathrm{diag}(5,2)$ 保留两条初值，显示 $[0,1]$ 的残差为零而特征值仍为次主的 2。 |
| 下一决策 | 此例不是一般谱误差界或随机重启保证；下一审计继续寻找“一个正确诊断遮蔽另一条前提”的核心课程断点，而不为专题均衡叠加相同报告。 |

## v5.29-a：MH 平稳核与有限步混合的总变差对照（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 让读者区分“详细平衡使目标分布平稳”与“某个有限步轨迹已混合”两个不同结论。 |
| 范围 | 深化[Metropolis–Hastings](/probability-ml/metropolis-hastings)：新增有限步分布传播、总变差距离报告与重放证书；用同一两状态核比较两个初值，并纳入优先深度文章门禁和成熟度看板。 |
| 验证 | `python -m unittest projects.naive_bayes_spam.test_metropolis_hastings` 通过 6 项测试；发布前运行全项目测试、内容/公式、搜索、无障碍与静态构建门禁。 |
| 差异 | 旧报告正确验证核的平稳性，却不能量化有限步初值偏差；新报告在一步后显示距离分别为 $1/4$ 与 $1/12$，两者仍未满足 $0.01$ 容差。 |
| 下一决策 | 此有限状态例不产生一般混合时间界；后续只处理能在共享输入上分离正确指标与遗漏前提的断点。 |

## v5.30-a：Jacobi 的对角占优与谱半径对照（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 让读者区分“严格对角占优的充分检查”与“Jacobi 谱半径小于一的收敛条件”。 |
| 范围 | 深化[迭代解线性方程组](/numerical-computing/iterative-linear-systems)：新增受限 $2\times2$ 谱半径/有限运行报告与证书，比较两个同为非对角占优的系统，并纳入优先深度文章门禁与成熟度看板。 |
| 验证 | `python -m unittest projects.floating_point_museum.test_linear_iterations` 通过 6 项测试；发布前运行全项目测试、内容/公式、搜索、无障碍与静态构建门禁。 |
| 差异 | 旧文用一个非占优发散例说明风险；新实验同时展示非占优而 $\rho=\sqrt{0.4}<1$ 的收敛例与 $\rho=2$ 的失败例，不能再把充分条件反推为必要条件。 |
| 下一决策 | 这个二维公式不估计一般矩阵谱半径；后续只审计具有同等级、可独立核对的概念断点。 |

## v5.31 Meta Review：从局部正确到结论范围（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 复核 v5.28–v5.30 是否仍在增加不同读者能力，并决定下一轮不应继续深化哪些已有闭环课程。 |
| 范围 | 对照幂迭代、MH 与 Jacobi 的局部指标/独立量/结论范围；审计条件数、Bellman–Ford、有限群与协方差的现有闭环；记录优先深度课、测试、KaTeX 页面和搜索索引基线。 |
| 验证 | 审计课程、实验、门禁与已通过的全站构建：110 篇正文、42 篇优先深度课、452 项测试、120 个 KaTeX 页面；搜索索引 1381.0 KiB，保留约 1.4 KiB。 |
| 差异 | 原选择规则要求共享输入和独立不变量；新规则进一步要求明确标注“局部正确量不可升级的结论范围”，并把至少 1 KiB 的搜索余量作为未来候选的构建前约束。 |
| 下一决策 | 不按专题配额选择 v5.32；只有同时满足真实概念断点、有限安全反例和索引预算约束的候选才能实施。 |

## v5.32-a：GitHub Pages 的 Node 24 发布链（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 消除 GitHub Pages 成功部署中持续出现的 Node 20 Action 弃用警告，降低旧运行时移除后的发布中断风险。 |
| 范围 | 将 `.github/workflows/deploy-pages.yml` 的 checkout、setup-node、upload-pages-artifact、deploy-pages 升至 Node 24 运行时主版本；保留站点 Node 22、权限、并发与构建命令。 |
| 验证 | 本地运行完整构建；远端 Pages 工作流必须成功完成 build/deploy，并检查工作流注释不再出现 Node 20 弃用信息。 |
| 差异 | 旧工作流虽能发布，却依赖被强制兼容的弃用 Action 运行时；新工作流将运行时升级与项目依赖 Node 版本解耦。 |
| 下一决策 | 这不是依赖全面升级；后续只在出现可证实的交付风险或教学断点时修改发布链。 |

## v5.33-a：低秩推荐的训练/留出误差对照（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 让读者在同一评分表上区分 ALS 的观测训练误差与已隐藏真实评分的留出误差。 |
| 范围 | 深化[低秩推荐](/linear-algebra/low-rank-recommendation)：新增显式留出划分、训练/留出 RMSE、重放证书与篡改拒绝测试，并纳入优先深度文章门禁和成熟度看板。 |
| 验证 | `python -m unittest projects.linear_algebra_lab.test_recommendation` 通过 3 项测试；发布前运行全项目测试、内容/公式、搜索、无障碍与静态构建门禁。 |
| 差异 | 旧实验只证明 ALS 重放观测拟合；新实验在同一原始表中显示训练 RMSE 约 $0.027$、留出 RMSE 约 $4.802$，训练成功不能被误作未见评分质量。 |
| 下一决策 | 该划分不是时间切分或产品评估；后续候选仍须满足独立量与搜索预算门槛。 |

## v5.34 Meta Review：以排除证据收紧下一课的立项门槛（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 在 v5.31 的结论范围规则后，复核近期可能被误判为薄弱的核心课程，避免把已有闭环再次包装成版本。 |
| 范围 | 审计重要性采样、椭圆曲线、预条件 CG、加权活动选择 DP、DH、数字签名与数值积分的正文、实验和测试。 |
| 验证 | 每门课程均具备共享输入上的独立量、有限反例、穷举/解析 oracle 或可重放证书；本轮不改变教学项目 API，也不将审计发现误写为新的数学结论。 |
| 差异 | 近期候选由“可能有工程边界”收紧为“正文、实验和测试之间存在可定位断裂”；没有这种断裂不立项。 |
| 下一决策 | 保留稳定基线，继续审计未进入该轮排除范围的核心链；仅在满足 v5.31 搜索容量与独立证据门槛时进入 v5.34-a。 |

### v5.34-a：SVD 的精确秩与数值秩缩放对照（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 区分非零奇异值定义的精确秩，与依赖尺度、误差模型和阈值的数值有效秩。 |
| 范围 | 在 SVD 课程与线性代数实验室加入绝对/相对阈值报告、单位缩放对照、重放证书与篡改拒绝测试，并加入优先深度门禁。 |
| 验证 | 受控谱保持精确秩 3；缩放后绝对阈值秩从 2 变 1，而相对阈值秩仍为 2；篡改缩放不变结论被拒绝。全项目 454 项测试、内容/公式、搜索、无障碍与静态构建均通过，索引为 1381.2 KiB。 |
| 差异 | 原课只有精确谱尾和参数量证书；新实验让读者观察到一个正确代数数量不足以选择带噪模型阶数。 |
| 下一决策 | 此相对阈值对照不建立噪声模型或生产秩判定器；下一轮继续审计尚未经过“精确性质与数值/语义决策”分离检查的核心课。 |

### v5.35-a：SCC 凝聚 DAG 与最大互达分块的独立审查（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 区分“跨分块图无环”与“分块确实是最大双向可达等价类”。 |
| 范围 | 为小图增加直接互达 oracle；在 SCC 课加入错误合并 `a→b` 仍能形成单点 DAG 的反例，并纳入优先深度门禁。 |
| 验证 | 正确 Kosaraju 分块通过；错误合并保持顶点覆盖却被互达关系拒绝。全站 455 项测试、内容/公式、渲染、无障碍与构建通过；索引为 1381.4 KiB，保留约 1 KiB 余量。 |
| 差异 | 原 `condensation_report.valid` 只证明给定组件间跨边可拓扑排序；新审查还验证组件划分本身的最大性。 |
| 下一决策 | 小图 oracle 不替代大图生产验证；下一轮继续寻找“局部结构证据不能确认全局对象”的不同断点，并保持搜索余量门槛。 |

### v5.36-a：矩阵结合律与浮点执行顺序的双路径对照（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 区分实数矩阵结合律与固定浮点累加顺序的有限执行结果，并修复非方阵乘法的形状检查。 |
| 范围 | 以整数值三矩阵比较精确/float 两条结合路径，增加重放证书和篡改拒绝测试；矩阵乘法课加入优先深度门禁。 |
| 验证 | 精确路径都为 1，左/右浮点路径为 1/0；合法非方阵路径不再被误拒绝，篡改精确结合字段被拒绝。全站 456 项测试、内容/公式、渲染、无障碍与构建通过；索引为 1381.2 KiB，保留约 1.2 KiB 余量。 |
| 差异 | 原课只有文字提醒；新实验将“舍入顺序差异不推翻代数定理”变为可运行的双路径证据。 |
| 下一决策 | 该构造不形成任何 BLAS/GPU 误差界；后续候选仍须以独立量和可守住的搜索预算证明增量价值。 |

### v5.37-a：并查集从内嵌示例到可重放路径压缩实验（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 兑现并查集课程承诺的路径压缩对照实验，并分开有限路径计数与均摊复杂度结论。 |
| 范围 | 新增算法实验室 `UnionFind`、受控父链报告与证书、三项单测；课程改用真实模块入口并纳入优先深度门禁。 |
| 验证 | 六节点链首次/未压缩重复查询均为 5 跳，压缩后重复查询为 1 跳；篡改路径长度被拒绝。全站 459 项测试、内容/公式、渲染、无障碍与构建通过；索引为 1381.0 KiB，保留约 1.4 KiB 余量。 |
| 差异 | 原课程只有可复制片段；新版本提供可测试、可重放的实验，并不把单条链的效果误称为均摊证明。 |
| 下一决策 | 该模块不支持删边、路径恢复或大规模性能基准；下一轮继续审计尚未兑现实验承诺的课程，且只实施满足独立证据门槛的增量。 |

### v5.38-a：一阶误差尺度与有限扰动交叉项对照（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 将“一阶敏感性不是有限扰动严格界”的文字边界升级为可重放反例。 |
| 范围 | 在误差分析项目加入乘积有限扰动报告/证书与篡改拒绝测试；正文、深度门禁和成熟度看板同步说明交叉项。 |
| 验证 | 461 项全项目测试、内容/公式、渲染、无障碍与构建通过；搜索索引为 1381.3 KiB，保留约 1.1 KiB 余量。 |
| 差异 | 旧课只有公式函数与分散断言；新报告将线性项、交叉项和实际变化绑定在同一输入。 |
| 下一决策 | 不将一个正向扰动反例误报为一般高阶界；发布后继续审计真实断点。 |

### v5.39-a：有限穷举与一般证明的反例边界（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 兑现逻辑课“小规模穷举寻找反例”的实验承诺，并分开有限搜索与一般证明。 |
| 范围 | 新增两种错误二分更新的有界反例搜索、重放证书与测试；同步正文、深度门禁和成熟度看板。 |
| 验证 | 464 项全项目测试、内容/公式、渲染、无障碍与构建通过；搜索索引为 1381.3 KiB，保留约 1.1 KiB 余量。 |
| 差异 | 旧课只有正确实例；新实验显示一个区间不收缩和一个实际目标被丢失的最小有限见证。 |
| 下一决策 | 有界穷举不能外推到任意长度；发布后继续审计实验兑现与结论范围。 |

### v5.40-a：蒙特卡洛导论接入可重放报告（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 兑现导论中圆周率重复与同目标重要性对照的实验承诺。 |
| 范围 | 用现有模拟和重要性采样报告替换内嵌单次函数，绑定测试入口与证书，并纳入优先深度门禁。 |
| 验证 | 464 项全项目测试、内容/公式、渲染、无障碍与构建通过；搜索索引为 1381.4 KiB，保留约 1 KiB 余量。 |
| 差异 | 旧正文只给出独立函数；新正文可重放多种子报告和同目标提议比较，并禁止跨目标方差排名。 |
| 下一决策 | 此入口不提供一般重要性采样误差界；发布后继续审计真正缺口。 |

### v5.41-a：复杂度实验的全序数值前提（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 防止 `NaN` 绕过双指针有序输入检查并破坏复杂度/正确性证明的比较前提。 |
| 范围 | 收紧实验的有限数值合同，添加 NaN/无穷输入回归测试，同步课程、门禁与成熟度看板。 |
| 验证 | 464 项全项目测试、内容/公式、渲染、无障碍与构建通过；搜索索引为 1381.4 KiB，保留约 1 KiB 余量。 |
| 差异 | 旧实现把 `[NaN,1]` 当作可搜索输入并返回结果；新实现明确拒绝无全序语义的输入。 |
| 下一决策 | 输入验证不替代复杂度证明；发布后继续检查其它算法的隐藏顺序或数值前提。 |

### v5.42-a：有限群教学枚举的统一上限（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 让有限群课声明的小素域枚举边界在所有公开 API 中一致生效。 |
| 范围 | 为模数与元素增加显式合同；统一 1000 上限，并为阶、生成元与报告 API 补充绕过回归测试。 |
| 验证 | 464 项全项目测试、内容/公式、渲染、无障碍与构建通过；搜索索引为 1381.4 KiB，保留约 1 KiB 余量。 |
| 差异 | 旧上限仅限制离散对数；其他枚举入口可处理超过课程承诺的模数。 |
| 下一决策 | 教学上限不构成密码参数建议；发布后继续审计 API 之间的边界一致性。 |

### v5.43 Meta Review：从局部输入契约回到跨层证据（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 复核 v5.37–v5.42 是否各自改变不同读者能力，并防止继续围绕相同输入契约制造版本。 |
| 范围 | 审计最新课程、项目、回归测试、边界说明与公开页；复核自动微分的 JVP/HVP/差分/故障检查已闭环。 |
| 验证 | 工作树干净；本轮全站门禁为 464 项测试、120 个 KaTeX 页面、1381.5 KiB 搜索索引；公开 Pages 页面已逐版验证。 |
| 差异 | 新结论把候选选择从局部 API 检查转向时间、簇与配对同时存在的纵向不确定性断点。 |
| 下一决策 | 仅当能用有限数据流分别展示趋势、簇相关与配对缺失的影响时，才创建下一实现版本。 |

### v5.44-a：配对纵向簇轨迹的校准差报告（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 为跨层重复簇提供不拆配对轨迹的有限、可重放替代合同。 |
| 范围 | 新增完整轨迹抽样、逐层 ECE 差与首末趋势区间、重放证书及不一致层序/篡改趋势测试；课程、项目页与项目 README 统一入口。 |
| 验证 | 466 项全项目测试、内容/公式、渲染、无障碍与构建通过；搜索索引为 1381.1 KiB，保留约 1.3 KiB 余量。 |
| 差异 | 旧课只能拒绝跨层重复 ID；新报告保留同一用户所有层及两侧窗口，并将趋势量绑定到同一重采样。 |
| 下一决策 | 共享层序与完整随访是受限合同，不替代不规则纵向、层级或因果模型。 |

### v5.45-a：类别漂移点距离与有限样本波动（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 区分分类输入漂移的经验 PSI/TV 点距离与有限窗口下的抽样波动。 |
| 范围 | 在冻结类别宇宙上新增观测级 bootstrap、区间重放证书、样本量对照与篡改拒绝；同步数据漂移课、项目页、README 和成熟度看板。 |
| 验证 | 469 项全项目测试、内容/公式、120 个 KaTeX 页面、搜索（1381.6 KiB）与无障碍静态构建均通过；发布后检查 GitHub Pages 工作流和公开课程页。 |
| 差异 | 同一经验比例的小/大窗口不再仅显示近似相同的 PSI/TV，还可观察不同的区间宽度。 |
| 边界 | 逐条重抽只依赖 i.i.d. 类别观测假设；不替代时间块、簇级、纵向、显著性、因果或自动行动设计。 |

### v5.46 Meta Review：抽样单位的证据范围（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 检查 v5.44–v5.45 是否在增加不同能力，避免把 bootstrap 变成可机械复制的区间模板。 |
| 范围 | 对照独立类别观测、带标签观测、时间块、簇、时间分层簇与配对纵向轨迹的源、课程入口、重放策略和边界。 |
| 证据 | 每类均公开自己的 `resampling_unit`；v5.45 的测试以相同经验比例、不同样本量证明点距离与区间宽度独立；当前 469 项项目测试和公开 Pages 均已验证。 |
| 差异 | 后续立项门槛从“是否能加入区间”提升为“是否能让第二条独立量、有限反例或失效前提改变读者的结论范围”。 |
| 下一决策 | 不再扩展概率 bootstrap 家族；跨 P0 专题审计局部正确量被过度外推的断点，只有通过新门槛才实现。 |

### v5.47-a：重采样原语的模块边界（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 移除类别、簇和纵向报告对“时间块校准”私有函数的概念无关依赖。 |
| 范围 | 新增指标中性的整数控制与百分位原语，迁移观测级、时间块、簇级、分层簇级、配对纵向和窗口比较报告；不改变任何抽样单位、合同或统计结论。 |
| 验证 | 共享原语有直接单测；所有既有报告继续以固定种子重放证书。471 项全项目测试、内容/公式、120 个 KaTeX 页面、搜索（1381.6 KiB）与无障碍静态构建均通过；发布后检查 GitHub Pages。 |
| 差异 | 修改时间块校准实现不再是其它重采样报告的隐含前置条件，抽样设计和通用控制各自有清晰归属。 |
| 下一决策 | 此维护增量不构成新的统计能力；后续回到跨专题的结论范围审计。 |

### v5.48-a：可复制 HMAC 课程示例（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 修复消息认证码课中调用 `hmac_tag` 却未导入的可复制示例错误。 |
| 范围 | 补齐示例导入，并以测试从 Markdown 提取并执行该完整首段 Python 代码块；不修改 HMAC、序号或安全策略语义。 |
| 验证 | 示例本身会验证正常标签、篡改消息和有效重放的不同结果；472 项全项目测试、内容/公式、120 个 KaTeX 页面、搜索（1381.5 KiB）与无障碍静态构建均通过；发布后检查 GitHub Pages。 |
| 差异 | 读者可直接运行课程中的代码，而非依赖隐式名称或手动猜测导入。 |
| 下一决策 | 将可运行课程片段视作交付面继续审计；只有能定位具体执行断裂的页面才增加同类回归测试。 |

### v5.49-a：同 MSE 的全局结构对照（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 兑现 SSIM 课“同样 MSE 看起来不同”的标题承诺，分开逐像素误差与全局结构统计量。 |
| 范围 | 新增 MSE 匹配的两份图像质量/SSIM 成对报告与证书；以恒定亮度偏移、交错噪声和同一 $4\times4$ 参考矩阵作受控对照；同步课程正文。 |
| 验证 | 报告拒绝 MSE 不匹配输入和篡改的 SSIM 排序；473 项全项目测试、内容/公式、120 个 KaTeX 页面、搜索（1381.8 KiB）与无障碍静态构建均通过；发布后检查 GitHub Pages。 |
| 边界 | 全局 SSIM 不是窗口 SSIM、视觉偏好、下游任务质量或自动行动规则。 |

### v5.50-a：LU 复用与重复消元的工作量对照（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 将 LU 课“多个右端才有复用收益”从渐近文字变为共享矩阵上的实际双路径报告。 |
| 范围 | 新增 PA=LU 重放、复用/重分解解向量、声明的消元/三角点积工作量与证书；同步课程正文。 |
| 验证 | 两条路径的解与 $PA=LU$ 一致；篡改节省量或不足两个右端均被拒绝。474 项全项目测试、内容/公式、120 个 KaTeX 页面、搜索（1381.9 KiB）与无障碍静态构建均通过；发布后检查 GitHub Pages。 |
| 边界 | 计数不等于 wall-clock 基准，且不覆盖主元搜索、除法、内存、稀疏填充或硬件效应。 |

### v5.51 Meta Review：从“有实验”到“实验兑现承诺”（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 判断 v5.47–v5.50 是否仍形成不同读者能力，并阻止将实现、公式或泛化指标误作课程交付。 |
| 范围 | 审计共享 bootstrap 原语、HMAC 代码块、同 MSE 的 SSIM 对照、LU 复用/重分解报告，以及 Floyd--Warshall 的现有闭环。 |
| 证据 | HMAC 代码块现从 Markdown 原样执行；SSIM 固定 MSE 并重放结构排序；LU 固定矩阵/右端并重放两条路径；Floyd--Warshall 已有状态、路径、负环和工作量证据。 |
| 差异 | 新立项门槛要求“课程承诺—共享输入—独立输出—篡改拒绝”完整对应，而非仅有可运行片段、复杂度文字或单一最终值。 |
| 下一决策 | 不为已闭环页面追加同类报告；跨 P0 专题审计仍由这条承诺兑现门槛决定。 |

### v5.52-a：总体—分层方向反转的可重放证据（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 让联合/边缘/条件分布课在同一份三维计数输入上区分总体关联与每层关联。 |
| 范围 | 新增严格整数计数合同、每层/总体成功率报告、方向反转判定和精确重放证书；同步课程中的公式、可运行代码与练习。 |
| 验证 | 两个分层均为 `exposed_higher`、汇总为 `exposed_lower`；证书拒绝改变总体方向或反转结论，478 项全项目测试、内容/公式、120 个 KaTeX 页面、搜索（1382.3 KiB）与无障碍静态构建均通过。 |
| 差异 | 读者不再只能从二维表得到单一边缘量，而能在共享计数上看到边缘化的权重如何改变结论方向。 |
| 边界 | 这是描述性关联而非因果效应、混杂控制、显著性检验或自动决策。 |

### v5.53-a：相似度排序结论的独立重放（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 让向量课的“方向相同”和“绝对量接近”会给出不同第一名这一承诺可独立审计。 |
| 范围 | 为同一查询/候选集上的余弦与欧氏排序新增重放证书；课程代码块直接验证证书，测试覆盖排名和度量范围篡改。 |
| 验证 | 479 项项目测试、内容/公式、120 个 KaTeX 页面、搜索（1382.1 KiB）与无障碍静态构建通过。 |
| 差异 | 排名分歧不再仅是函数生成时的断言，外部报告不能任意改写第一名或把选择边界扩大为通用规则。 |
| 边界 | 证书不替业务选择相似度，也不把有限向量排序升级为检索质量或产品价值。 |

### v5.54-a：双指针的安全舍弃轨迹（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 让渐进复杂度课的双指针正确性不再只依赖最终下标和比较计数。 |
| 范围 | 新增有序数组双和逐步区间报告、舍弃端点、操作上界/模型与独立重放证书；保留原 `two_sum_sorted_trace` API。 |
| 验证 | 成功/未找到路径均满足最多 $n-1$ 次比较；篡改舍弃动作或上界被拒绝。480 项项目测试、内容/公式、120 个 KaTeX 页面、搜索（1382.3 KiB）与无障碍静态构建通过。 |
| 差异 | “每次移动一个指针”从文字不变量变为可检查的中间状态，而精确比较数仍明确不是墙钟性能。 |
| 边界 | 仅适用于有限、非降、可比较数值数组；不涵盖排序成本、缓存/I/O、并行或一般搜索算法。 |

### v5.55-a：样本标准差区间的覆盖率重放（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 将置信区间课的内嵌正态覆盖率模拟升级为可测试、可重放的项目产物。 |
| 范围 | 新增固定种子正态样本均值报告、样本标准差区间、经验覆盖率/平均宽度和独立证书；课程直接消费该产物。 |
| 验证 | 固定输入 $(\mu,\sigma,n,T,seed,z)=(10,4,80,1000,7,1.96)$ 的覆盖率为 0.957，篡改覆盖率会被拒绝。481 项项目测试、内容/公式、120 个 KaTeX 页面、搜索（1381.9 KiB）与无障碍静态构建通过。 |
| 差异 | 覆盖率不再只是页面中的一次打印，而是绑定模型、随机性和区间规则的可复查教学证据。 |
| 边界 | 仅说明声明的 i.i.d. 正态课堂模型；不证明一般覆盖率、现实抽样代表性、因果或自动决策。 |

### v5.56-a：中心化 PCA 与原始二阶矩的方向分离（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 将“PCA 必须先中心化”的文字警告变为同一数据上的可运行方向反例。 |
| 范围 | 新增二维中心化/未中心化二阶矩比较报告和证书；课程以远离原点、仅沿另一轴变化的三点样本并列两条方向。 |
| 验证 | 中心化分量沿 $y$、未中心化分量沿 $x$，未中心化方向更贴近均值；篡改该结论被拒绝。483 项项目测试、内容/公式、120 个 KaTeX 页面、搜索（1381.8 KiB）与无障碍静态构建通过。 |
| 差异 | 读者可区分“原始二阶矩的合法特征分解”与“围绕样本均值最大方差的 PCA”，不再把差异写成算法对错。 |
| 边界 | 仅限二维、非零均值有限样本；不处理标准化、异常值、稳健 PCA、高维统计或任务效用。 |

### v5.57-a：二次消去比较的外部验证（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 让算法稳定性课的直接/稳定二次公式对照不再信任报告内部布尔标记。 |
| 范围 | 新增从系数重放两条 binary64 路径、十进制参考、小根误差与消去结论的验证器；补齐篡改误差/结论测试和课程入口。 |
| 验证 | 对 $(1,10^8,1)$，直接路径暴露消去、稳定路径达到高精度；篡改误差或结论会被拒绝。484 项项目测试、内容/公式、120 个 KaTeX 页面、搜索（1382.0 KiB）与无障碍静态构建通过。 |
| 差异 | 数值对照从“报告自称稳定”变成同一系数和参考路径上的可重放结论。 |
| 边界 | 仅覆盖有限实根二次式与课堂十进制参考；不替代缩放、复根、上溢处理或生产求根库。 |

### v5.58-a：岭正则化改变秩亏拟合的目标（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 让读者区分“数值上更好地求解普通最小二乘”与“以较小系数为偏好、定义新的岭目标”。 |
| 范围 | 新增可接受秩亏/宽矩阵的教学级 ridge 求解与报告；课程以重复列数据展示数据梯度、惩罚梯度和总梯度的抵消。 |
| 验证 | 对 $A=[(1,1),(2,2),(3,3)]^T,b=(1,2,4)^T,\lambda=1$，解为 $(17/29,17/29)$，正则化梯度接近零而数据梯度显著非零；新增项目测试覆盖零正则拒绝。 |
| 差异 | 原问题的非唯一性不再只作为 SVD 的旁注；读者可运行地看到“唯一解”来自新增偏好，而不是 QR 或消元修复了数据可辨识性。 |
| 边界 | `lambda` 不是由该函数自动决定；教学路径显式形成移位正规方程，不应替代生产环境中稳定的 QR/SVD 型 ridge 求解器、验证集选择或领域审查。 |

### v5.59-a：冻结校准审计的组宇宙（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 修复“预先定义子群”在分组校准文章中有文字要求、实现却从当前窗口推断组集合的断裂。 |
| 范围 | 将分组校准合同升级为 v2，要求 `declared_groups`；报告按声明顺序输出全部组，拒绝未声明观测组，并将零样本组保留为无指标的证据不足行。 |
| 验证 | 新测试验证覆盖组与零样本声明组共同输出、未知组被拒绝；篡改报告中的声明组宇宙被证书拒绝。 |
| 差异 | 当前窗口不再决定“谁有资格被审计”；缺失覆盖成为可见结果，而不再被误读为不存在该群体。 |
| 边界 | 这只是治理后的描述性比较合同，不选择敏感属性、不处理多重比较、不建立因果/公平结论，也不自动触发模型或服务行动。 |

### v5.60-a：局部 SSIM 定位固定平铺中的最差区域（已完成）

| 字段 | 记录 |
| --- | --- |
| 目标 | 补齐全局 SSIM 无法指出局部质量缺陷位置的学习断点。 |
| 范围 | 新增完整不重叠窗口的灰度 SSIM 报告、最差窗口坐标与重放验证；窗口尺寸必须整除图像。 |
| 验证 | 4×4 常量图仅损坏左上 2×2 时，报告定位 `(0,0)`，其局部 SSIM 低于全局 SSIM；篡改位置或不完整平铺被拒绝。 |
| 差异 | 读者不再把整图分数误读为每个区域都同样好，可将数值质量问题定位到固定窗口。 |
| 边界 | 不实现滑动/多尺度/颜色感知 SSIM，不处理图像编解码，也不推出人类感知或下游任务结论。 |
