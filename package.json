{
  "name": "computer-mathematics",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22.4.0"
  },
  "scripts": {
    "dev": "vitepress dev docs",
    "content:check": "node scripts/check-content.mjs",
    "projects:test": "python -m unittest discover -s projects -t . -p \"test_*.py\"",
    "build": "npm run content:check && npm run projects:test && vitepress build docs && node scripts/prepare-sites.mjs",
    "preview": "vitepress preview docs"
  },
  "devDependencies": {
    "katex": "^0.18.4",
    "markdown-it-katex": "^2.0.3",
    "vitepress": "^1.6.4"
  },
  "type": "module"
}
