# 数学预备实验室

`summation.py` 将有限求和的半开区间约定、循环累加与平方和闭式并列，并以可运行证书检查它们在小整数输入上相符。`sets_arrays.py` 则把有限集合的并、交、差与二维数组的矩形形状、零基坐标和值绑定进同一份可重放报告。

它是五大专题共享的入门实验，不依赖 NumPy；规模化数组计算应在理解成员关系、索引与形状后交给 NumPy/PyTorch 等库。
