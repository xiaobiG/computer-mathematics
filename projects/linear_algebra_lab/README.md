# 线性代数实验室

运行 `python -m unittest discover -s projects/linear_algebra_lab -p "test_*.py"`。模块实现矩阵乘法、带审计轨迹的部分选主元高斯消元、可重构验证的 $PA=LU$ 分解、解析梯度与中心差分对照、向量投影与带残差轨迹的幂迭代，以及精确截断 SVD 的谱尾误差与低秩参数量证书，服务于对应的 Markdown 课程正文。
