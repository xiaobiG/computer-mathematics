# 线性代数实验室

运行 `python -m unittest discover -s projects/linear_algebra_lab -p "test_*.py"`。模块实现矩阵乘法、列独立性与基坐标重构证书、带主元/行变换重放证书的部分选主元高斯消元、正规方程与 QR 的最小二乘对照，以及让读者构造 $A,b$ 后诊断精确可达、投影、秩亏或欠定路径的最小二乘入口；双数前向 JVP 与受限计算图反向 VJP 的解析梯度对照；可独立重放的二维 PCA 中心化—协方差—投影—重构报告；向量投影与带残差轨迹的幂迭代；余弦相似度与欧氏距离的候选排序对照；以及精确截断 SVD 的谱尾误差、低秩参数量证书、压缩域图像检索报告、可重放的 MSE/RMSE/PSNR 图像误差报告、固定种子的随机范围发现证书和只在观测评分上拟合的秩一 ALS 轨迹证书，服务于对应的 Markdown 课程正文。
